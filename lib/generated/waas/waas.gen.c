// waas v0.1.0 2090ac211dc48d91540b37e30ff523bf191caf04
// --
// Code generated by webrpc-gen@v0.36.0 with /Users/tarikan/Development/0xsequence/gen-c generator. DO NOT EDIT.
//
// webrpc-gen -schema=waas.ridl -target=/Users/tarikan/Development/0xsequence/gen-c -emit=impl -header=waas.gen.h -out=/tmp/waas.gen.c

#include "waas.gen.h"

#include <curl/curl.h>
#include <errno.h>
#include <inttypes.h>
#include <json-c/json.h>
#include <limits.h>
#include <stdio.h>

typedef struct {
    char *data;
    size_t len;
    size_t cap;
} waas_buffer;

#if defined(__GNUC__) || defined(__clang__)
#define WAAS_JSON_UNUSED __attribute__((unused))
#else
#define WAAS_JSON_UNUSED
#endif

static WAAS_JSON_UNUSED int waas_json_is_null(const struct json_object *value) {
    return value == NULL;
}

static WAAS_JSON_UNUSED int waas_json_is_object(const struct json_object *value) {
    return value != NULL && json_object_is_type(value, json_type_object);
}

static WAAS_JSON_UNUSED int waas_json_is_array(const struct json_object *value) {
    return value != NULL && json_object_is_type(value, json_type_array);
}

static WAAS_JSON_UNUSED int waas_json_is_string(const struct json_object *value) {
    return value != NULL && json_object_is_type(value, json_type_string);
}

static WAAS_JSON_UNUSED int waas_json_is_bool(const struct json_object *value) {
    return value != NULL && json_object_is_type(value, json_type_boolean);
}

static WAAS_JSON_UNUSED int waas_json_is_true(const struct json_object *value) {
    return value != NULL && json_object_get_boolean((struct json_object *)value);
}

static WAAS_JSON_UNUSED int waas_json_is_number(const struct json_object *value) {
    return value != NULL &&
           (json_object_is_type(value, json_type_int) ||
            json_object_is_type(value, json_type_double));
}

static WAAS_JSON_UNUSED struct json_object *waas_json_parse(const char *text) {
    enum json_tokener_error err = json_tokener_success;
    return json_tokener_parse_verbose(text, &err);
}

static WAAS_JSON_UNUSED void waas_json_release(struct json_object *value) {
    if (value) {
        json_object_put(value);
    }
}

static WAAS_JSON_UNUSED int waas_json_add_object_member(
    struct json_object *object_json,
    const char *key,
    struct json_object *item_json
) {
    if (!object_json || !json_object_is_type(object_json, json_type_object)) {
        if (item_json) {
            json_object_put(item_json);
        }
        return 0;
    }
    if (json_object_object_add(object_json, key, item_json) != 0) {
        if (item_json) {
            json_object_put(item_json);
        }
        return 0;
    }
    return 1;
}

static WAAS_JSON_UNUSED int waas_json_add_array_item(
    struct json_object *array_json,
    struct json_object *item_json
) {
    if (!array_json || !json_object_is_type(array_json, json_type_array)) {
        if (item_json) {
            json_object_put(item_json);
        }
        return 0;
    }
    if (json_object_array_add(array_json, item_json) != 0) {
        if (item_json) {
            json_object_put(item_json);
        }
        return 0;
    }
    return 1;
}

static WAAS_JSON_UNUSED struct json_object *waas_json_array_item(
    const struct json_object *array_json,
    int index
) {
    if (!array_json || !json_object_is_type(array_json, json_type_array) || index < 0) {
        return NULL;
    }
    return json_object_array_get_idx(array_json, (size_t)index);
}

static WAAS_JSON_UNUSED int waas_json_array_size(const struct json_object *array_json) {
    if (!array_json || !json_object_is_type(array_json, json_type_array)) {
        return 0;
    }
    return (int)json_object_array_length(array_json);
}

static WAAS_JSON_UNUSED int waas_json_object_get(
    const struct json_object *object_json,
    const char *key,
    struct json_object **out
) {
    struct json_object *value = NULL;

    if (out) {
        *out = NULL;
    }
    if (!object_json || !json_object_is_type(object_json, json_type_object)) {
        return 0;
    }
    if (!json_object_object_get_ex(object_json, key, &value)) {
        return 0;
    }
    if (out) {
        *out = value;
    }
    return 1;
}

static WAAS_JSON_UNUSED const char *waas_json_string_value(const struct json_object *value) {
    if (!waas_json_is_string(value)) {
        return NULL;
    }
    return json_object_get_string((struct json_object *)value);
}

static WAAS_JSON_UNUSED int waas_json_parse_value(
    const char *text,
    struct json_object **out
) {
    enum json_tokener_error err = json_tokener_success;
    struct json_object *parsed = json_tokener_parse_verbose(text, &err);

    if (out) {
        *out = NULL;
    }
    if (err != json_tokener_success) {
        return 0;
    }
    if (out) {
        *out = parsed;
    } else if (parsed) {
        json_object_put(parsed);
    }
    return 1;
}

static WAAS_JSON_UNUSED char *waas_json_serialize(struct json_object *value) {
    const char *text;
    size_t len = 0;
    char *copy;

    if (!value) {
        return waas_strdup("null");
    }

    text = json_object_to_json_string_ext(value, JSON_C_TO_STRING_PLAIN);
    if (!text) {
        return NULL;
    }
    len = strlen(text);

    copy = (char *)malloc(len + 1);
    if (!copy) {
        return NULL;
    }
    memcpy(copy, text, len + 1);
    return copy;
}

static WAAS_JSON_UNUSED int waas_json_get_int64_exact(
    const struct json_object *value,
    int64_t *out
) {
    const char *text;
    char *end = NULL;
    intmax_t parsed;

    if (!out || !value || !json_object_is_type(value, json_type_int)) {
        return 0;
    }
    text = json_object_get_string((struct json_object *)value);
    if (!text || text[0] == '\0') {
        return 0;
    }

    errno = 0;
    parsed = strtoimax(text, &end, 10);
    if (errno == ERANGE || !end || *end != '\0') {
        return 0;
    }
    *out = (int64_t)parsed;
    return 1;
}

static WAAS_JSON_UNUSED int waas_json_get_uint64_exact(
    const struct json_object *value,
    uint64_t *out
) {
    const char *text;
    char *end = NULL;
    uintmax_t parsed;

    if (!out || !value || !json_object_is_type(value, json_type_int)) {
        return 0;
    }
    text = json_object_get_string((struct json_object *)value);
    if (!text || text[0] == '\0' || text[0] == '-') {
        return 0;
    }

    errno = 0;
    parsed = strtoumax(text, &end, 10);
    if (errno == ERANGE || !end || *end != '\0') {
        return 0;
    }
    *out = (uint64_t)parsed;
    return 1;
}

static WAAS_JSON_UNUSED int waas_json_get_double(
    const struct json_object *value,
    double *out
) {
    double parsed;

    if (!out || !waas_json_is_number(value)) {
        return 0;
    }

    errno = 0;
    parsed = json_object_get_double(value);
    if (errno == ERANGE) {
        return 0;
    }
    *out = parsed;
    return 1;
}
static void waas_set_error(
    waas_error *error,
    int code,
    int http_status,
    const char *name,
    const char *message,
    const char *cause
) {
    if (!error) return;
    waas_error_free(error);
    error->code = code;
    error->http_status = http_status;
    error->name = waas_strdup(name ? name : "WebrpcError");
    error->message = waas_strdup(message ? message : "request failed");
    error->cause = cause ? waas_strdup(cause) : NULL;
}

static int waas_buffer_grow(waas_buffer *buf, size_t need) {
    if (buf->cap >= need) return 1;
    size_t new_cap = buf->cap ? buf->cap : 1024;
    while (new_cap < need) new_cap *= 2;
    char *next = (char *)realloc(buf->data, new_cap);
    if (!next) return 0;
    buf->data = next;
    buf->cap = new_cap;
    return 1;
}

static size_t waas_write_cb(char *ptr, size_t size, size_t nmemb, void *userdata) {
    size_t n = size * nmemb;
    waas_buffer *buf = (waas_buffer *)userdata;
    if (!waas_buffer_grow(buf, buf->len + n + 1)) return 0;
    memcpy(buf->data + buf->len, ptr, n);
    buf->len += n;
    buf->data[buf->len] = '\0';
    return n;
}

static char *waas_join_url(const char *base_url, const char *path) {
    if (!base_url) return NULL;
    if (!path) path = "";

    size_t base_len = strlen(base_url);
    size_t path_len = strlen(path);
    int base_has_slash = base_len > 0 && base_url[base_len - 1] == '/';
    int path_has_slash = path_len > 0 && path[0] == '/';
    size_t cap = base_len + path_len + 2;
    char *out = (char *)malloc(cap);
    if (!out) return NULL;

    if (base_len == 0) {
        snprintf(out, cap, "%s", path);
    } else if (path_len == 0) {
        snprintf(out, cap, "%s", base_url);
    } else if (base_has_slash && path_has_slash) {
        snprintf(out, cap, "%.*s%s", (int)(base_len - 1), base_url, path);
    } else if (!base_has_slash && !path_has_slash) {
        snprintf(out, cap, "%s/%s", base_url, path);
    } else {
        snprintf(out, cap, "%s%s", base_url, path);
    }
    return out;
}

static int waas_append_header(struct curl_slist **headers, const char *header_value) {
    struct curl_slist *next;

    if (!headers || !header_value) return 0;
    next = curl_slist_append(*headers, header_value);
    if (!next) return 0;
    *headers = next;
    return 1;
}

static int waas_header_name_equals(const char *header, const char *name) {
    size_t i = 0;

    if (!header || !name) return 0;

    while (header[i] != '\0' && header[i] != ':' && name[i] != '\0' && name[i] != ':') {
        unsigned char h = (unsigned char)header[i];
        unsigned char n = (unsigned char)name[i];
        if (h >= 'A' && h <= 'Z') h = (unsigned char)(h - 'A' + 'a');
        if (n >= 'A' && n <= 'Z') n = (unsigned char)(n - 'A' + 'a');
        if (h != n) return 0;
        i++;
    }

    while (header[i] == ' ' || header[i] == '\t') {
        i++;
    }

    while (name[i] == ' ' || name[i] == '\t') {
        i++;
    }

    return (name[i] == '\0' || name[i] == ':') && (header[i] == '\0' || header[i] == ':');
}

static int waas_prepared_request_has_header(const waas_prepared_request *request, const char *name) {
    if (!request || !name || !request->headers) return 0;

    for (size_t i = 0; i < request->headers_count; ++i) {
        if (request->headers[i] && waas_header_name_equals(request->headers[i], name)) {
            return 1;
        }
    }

    return 0;
}

static int waas_prepared_request_overrides_header(const waas_prepared_request *request, const char *header_line) {
    if (!request || !header_line) return 0;

    if (waas_header_name_equals(header_line, "Content-Type")) {
        if ((request->content_type && request->content_type[0] != '\0') ||
            waas_prepared_request_has_header(request, "Content-Type")) {
            return 1;
        }
    }

    if (!request->headers) return 0;
    for (size_t i = 0; i < request->headers_count; ++i) {
        if (request->headers[i] &&
            waas_header_name_equals(request->headers[i], header_line)) {
            return 1;
        }
    }

    return 0;
}

static int waas_http_send_request(
    const char *base_url,
    const waas_prepared_request *request,
    const char *bearer_token,
    const struct curl_slist *default_headers,
    long timeout_ms,
    waas_http_response *response,
    waas_error *error
) {
    const char *http_method;
    waas_http_response result;
    int has_content_type = 0;
    int has_authorization = 0;
    waas_http_response_init(&result);

    if (!request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "request and response must be non-NULL", NULL);
        return -1;
    }

    if (curl_global_init(CURL_GLOBAL_DEFAULT) != 0) {
        waas_set_error(error, 0, 0, "TransportError", "curl_global_init failed", NULL);
        return -1;
    }

    CURL *curl = curl_easy_init();
    if (!curl) {
        waas_set_error(error, 0, 0, "TransportError", "curl_easy_init failed", NULL);
        return -1;
    }

    char *url = waas_join_url(base_url, request->path ? request->path : "");
    if (!url) {
        waas_set_error(error, 0, 0, "TransportError", "failed to build URL", NULL);
        curl_easy_cleanup(curl);
        return -1;
    }

    struct curl_slist *headers = NULL;
    for (const struct curl_slist *it = default_headers; it; it = it->next) {
        if ((bearer_token && bearer_token[0] != '\0' && waas_header_name_equals(it->data, "Authorization")) ||
            waas_prepared_request_overrides_header(request, it->data)) {
            continue;
        }
        if (!waas_append_header(&headers, it->data)) {
            waas_set_error(error, 0, 0, "TransportError", "failed to copy request headers", NULL);
            curl_slist_free_all(headers);
            free(url);
            curl_easy_cleanup(curl);
            return -1;
        }
        if (waas_header_name_equals(it->data, "Content-Type")) {
            has_content_type = 1;
        }
        if (waas_header_name_equals(it->data, "Authorization")) {
            has_authorization = 1;
        }
    }

    if (request->content_type && request->content_type[0] != '\0') {
        size_t need = strlen("Content-Type: ") + strlen(request->content_type) + 1;
        char *content_type_header = (char *)malloc(need);
        if (!content_type_header) {
            waas_set_error(error, 0, 0, "TransportError", "failed to allocate content-type header", NULL);
            curl_slist_free_all(headers);
            free(url);
            curl_easy_cleanup(curl);
            return -1;
        }
        snprintf(content_type_header, need, "Content-Type: %s", request->content_type);
        if (!waas_append_header(&headers, content_type_header)) {
            waas_set_error(error, 0, 0, "TransportError", "failed to append content-type header", NULL);
            free(content_type_header);
            curl_slist_free_all(headers);
            free(url);
            curl_easy_cleanup(curl);
            return -1;
        }
        free(content_type_header);
        has_content_type = 1;
    }

    for (size_t i = 0; i < request->headers_count; ++i) {
        if (!request->headers || !request->headers[i]) {
            waas_set_error(error, 0, 0, "TransportError", "request headers are invalid", NULL);
            curl_slist_free_all(headers);
            free(url);
            curl_easy_cleanup(curl);
            return -1;
        }
        if (!waas_append_header(&headers, request->headers[i])) {
            waas_set_error(error, 0, 0, "TransportError", "failed to append request header", NULL);
            curl_slist_free_all(headers);
            free(url);
            curl_easy_cleanup(curl);
            return -1;
        }
        if (waas_header_name_equals(request->headers[i], "Content-Type")) {
            has_content_type = 1;
        }
        if (waas_header_name_equals(request->headers[i], "Authorization")) {
            has_authorization = 1;
        }
    }

    if (!has_content_type && !waas_append_header(&headers, "Content-Type: application/json")) {
        waas_set_error(error, 0, 0, "TransportError", "failed to allocate request headers", NULL);
        curl_slist_free_all(headers);
        free(url);
        curl_easy_cleanup(curl);
        return -1;
    }
    if (!has_content_type) {
        has_content_type = 1;
    }

    if (bearer_token && bearer_token[0] != '\0' && !has_authorization) {
        size_t need = strlen("Authorization: Bearer ") + strlen(bearer_token) + 1;
        char *auth_header = (char *)malloc(need);
        if (!auth_header) {
            waas_set_error(error, 0, 0, "TransportError", "failed to allocate auth header", NULL);
            curl_slist_free_all(headers);
            free(url);
            curl_easy_cleanup(curl);
            return -1;
        }
        snprintf(auth_header, need, "Authorization: Bearer %s", bearer_token);
        if (!waas_append_header(&headers, auth_header)) {
            waas_set_error(error, 0, 0, "TransportError", "failed to append auth header", NULL);
            free(auth_header);
            curl_slist_free_all(headers);
            free(url);
            curl_easy_cleanup(curl);
            return -1;
        }
        free(auth_header);
        has_authorization = 1;
    }

    waas_buffer buf;
    memset(&buf, 0, sizeof(buf));
    http_method = request->http_method && request->http_method[0] != '\0' ? request->http_method : "POST";

    curl_easy_setopt(curl, CURLOPT_URL, url);
    curl_easy_setopt(curl, CURLOPT_HTTPHEADER, headers);
    curl_easy_setopt(curl, CURLOPT_CUSTOMREQUEST, http_method);
    curl_easy_setopt(curl, CURLOPT_POSTFIELDS, request->body ? request->body : "");
    curl_easy_setopt(curl, CURLOPT_POSTFIELDSIZE, (long)(request->body ? request->body_len : 0));
    curl_easy_setopt(curl, CURLOPT_TIMEOUT_MS, timeout_ms > 0 ? timeout_ms : 10000L);
    curl_easy_setopt(curl, CURLOPT_FOLLOWLOCATION, 1L);
    curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, waas_write_cb);
    curl_easy_setopt(curl, CURLOPT_WRITEDATA, &buf);

    CURLcode rc = curl_easy_perform(curl);
    if (rc != CURLE_OK) {
        waas_set_error(error, 0, 0, "TransportError", "HTTP request failed", curl_easy_strerror(rc));
    } else {
        curl_easy_getinfo(curl, CURLINFO_RESPONSE_CODE, &result.status_code);
        result.body = buf.data ? buf.data : waas_strdup("");
        result.body_len = buf.len;
        buf.data = NULL;
    }

    free(buf.data);
    curl_slist_free_all(headers);
    free(url);
    curl_easy_cleanup(curl);
    if (rc != CURLE_OK) {
        waas_http_response_free(&result);
        return -1;
    }

    *response = result;
    memset(&result, 0, sizeof(result));
    return 0;
}

static void waas_parse_rpc_error(const char *body, long http_status, waas_error *error) {
    struct json_object *error_name = NULL;
    struct json_object *error_code = NULL;
    struct json_object *error_msg = NULL;
    struct json_object *error_cause = NULL;
    struct json_object *error_status = NULL;
    int64_t parsed_code = 0;
    int64_t parsed_status = (int64_t)http_status;

    if (!body || body[0] == '\0') {
        waas_set_error(error, 0, (int)http_status, "WebrpcRequestFailed", "request failed", NULL);
        return;
    }

    struct json_object *root = waas_json_parse(body);
    if (!root || !waas_json_is_object(root)) {
        if (root) waas_json_release(root);
        waas_set_error(error, 0, (int)http_status, "WebrpcRequestFailed", "request failed", body);
        return;
    }

    waas_json_object_get(root, "error", &error_name);
    waas_json_object_get(root, "code", &error_code);
    waas_json_object_get(root, "msg", &error_msg);
    waas_json_object_get(root, "cause", &error_cause);
    waas_json_object_get(root, "status", &error_status);
    if (!waas_json_is_null(error_code)) {
        (void)waas_json_get_int64_exact(error_code, &parsed_code);
    }
    if (!waas_json_is_null(error_status)) {
        (void)waas_json_get_int64_exact(error_status, &parsed_status);
    }

    waas_set_error(
        error,
        (int)parsed_code,
        (int)parsed_status,
        waas_json_string_value(error_name) ? waas_json_string_value(error_name) : "WebrpcRequestFailed",
        waas_json_string_value(error_msg) ? waas_json_string_value(error_msg) : "request failed",
        waas_json_string_value(error_cause)
    );
    waas_json_release(root);
}
static WAAS_JSON_UNUSED struct json_object *waas_identity_to_json(const waas_identity *value);
static WAAS_JSON_UNUSED int waas_identity_from_json(const struct json_object *json, waas_identity *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_key_to_json(const waas_key *value);
static WAAS_JSON_UNUSED int waas_key_from_json(const struct json_object *json, waas_key *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_commit_verifier_params_to_json(const waas_commit_verifier_params *value);
static WAAS_JSON_UNUSED int waas_commit_verifier_params_from_json(const struct json_object *json, waas_commit_verifier_params *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_complete_auth_params_to_json(const waas_complete_auth_params *value);
static WAAS_JSON_UNUSED int waas_complete_auth_params_from_json(const struct json_object *json, waas_complete_auth_params *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_create_wallet_params_to_json(const waas_create_wallet_params *value);
static WAAS_JSON_UNUSED int waas_create_wallet_params_from_json(const struct json_object *json, waas_create_wallet_params *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_use_wallet_params_to_json(const waas_use_wallet_params *value);
static WAAS_JSON_UNUSED int waas_use_wallet_params_from_json(const struct json_object *json, waas_use_wallet_params *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_sign_message_params_to_json(const waas_sign_message_params *value);
static WAAS_JSON_UNUSED int waas_sign_message_params_from_json(const struct json_object *json, waas_sign_message_params *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_send_transaction_params_to_json(const waas_send_transaction_params *value);
static WAAS_JSON_UNUSED int waas_send_transaction_params_from_json(const struct json_object *json, waas_send_transaction_params *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_wallet_to_json(const waas_wallet *value);
static WAAS_JSON_UNUSED int waas_wallet_from_json(const struct json_object *json, waas_wallet *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_transaction_response_to_json(const waas_transaction_response *value);
static WAAS_JSON_UNUSED int waas_transaction_response_from_json(const struct json_object *json, waas_transaction_response *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_credential_data_to_json(const waas_credential_data *value);
static WAAS_JSON_UNUSED int waas_credential_data_from_json(const struct json_object *json, waas_credential_data *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_wallet_binding_data_to_json(const waas_wallet_binding_data *value);
static WAAS_JSON_UNUSED int waas_wallet_binding_data_from_json(const struct json_object *json, waas_wallet_binding_data *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_eoaw_allet_data_to_json(const waas_eoaw_allet_data *value);
static WAAS_JSON_UNUSED int waas_eoaw_allet_data_from_json(const struct json_object *json, waas_eoaw_allet_data *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_smart_wallet_data_to_json(const waas_smart_wallet_data *value);
static WAAS_JSON_UNUSED int waas_smart_wallet_data_from_json(const struct json_object *json, waas_smart_wallet_data *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_account_data_to_json(const waas_account_data *value);
static WAAS_JSON_UNUSED int waas_account_data_from_json(const struct json_object *json, waas_account_data *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_access_token_to_json(const waas_access_token *value);
static WAAS_JSON_UNUSED int waas_access_token_from_json(const struct json_object *json, waas_access_token *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_session_data_to_json(const waas_session_data *value);
static WAAS_JSON_UNUSED int waas_session_data_from_json(const struct json_object *json, waas_session_data *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_credential_info_to_json(const waas_credential_info *value);
static WAAS_JSON_UNUSED int waas_credential_info_from_json(const struct json_object *json, waas_credential_info *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_list_access_params_to_json(const waas_list_access_params *value);
static WAAS_JSON_UNUSED int waas_list_access_params_from_json(const struct json_object *json, waas_list_access_params *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_list_access_result_to_json(const waas_list_access_result *value);
static WAAS_JSON_UNUSED int waas_list_access_result_from_json(const struct json_object *json, waas_list_access_result *out, waas_error *error);
static WAAS_JSON_UNUSED struct json_object *waas_revoke_access_params_to_json(const waas_revoke_access_params *value);
static WAAS_JSON_UNUSED int waas_revoke_access_params_from_json(const struct json_object *json, waas_revoke_access_params *out, waas_error *error);


static WAAS_JSON_UNUSED struct json_object *waas_identity_to_json(const waas_identity *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_identity_type_to_string(value->type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "type", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->issuer) {
        field_json = json_object_new_string(value->issuer);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "iss", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->subject) {
        field_json = json_object_new_string(value->subject);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "sub", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->email) {
        field_json = json_object_new_string(value->email);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "email", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_identity_from_json(const struct json_object *json, waas_identity *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "type", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field type", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_identity_type_from_string(waas_json_string_value(field_json), &out->type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "iss", &field_json);
        if (field_present && !waas_json_is_null(field_json)) {
    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->issuer = waas_strdup(waas_json_string_value(field_json));
    if (!out->issuer) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "sub", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field sub", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->subject = waas_strdup(waas_json_string_value(field_json));
    if (!out->subject) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "email", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field email", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->email = waas_strdup(waas_json_string_value(field_json));
    if (!out->email) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_identity_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_key_to_json(const waas_key *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_key_type_to_string(value->key_type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "keyType", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->address) {
        field_json = json_object_new_string(value->address);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "address", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_key_from_json(const struct json_object *json, waas_key *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "keyType", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field keyType", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_key_type_from_string(waas_json_string_value(field_json), &out->key_type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "address", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field address", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->address = waas_strdup(waas_json_string_value(field_json));
    if (!out->address) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_key_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_commit_verifier_params_to_json(const waas_commit_verifier_params *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_identity_type_to_string(value->identity_type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "identityType", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_auth_mode_to_string(value->auth_mode));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "authMode", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    {
        struct json_object *object_json = json_object_new_object();
        if (!object_json) goto fail;
        for (size_t i = 0; i < value->metadata.count; ++i) {
            struct json_object *entry_json = NULL;

    if (value->metadata.values[i]) {
        entry_json = json_object_new_string(value->metadata.values[i]);
        if (!entry_json) goto fail;
    } else {
        entry_json = NULL;
    }
            if (!value->metadata.keys[i]) {
                waas_json_release(entry_json);
                waas_json_release(object_json);
                goto fail;
            }
            if (!waas_json_add_object_member(object_json, value->metadata.keys[i], entry_json)) {
                waas_json_release(object_json);
                goto fail;
            }
        }
        field_json = object_json;
    }
            if (!waas_json_add_object_member(root, "metadata", field_json)) goto fail;
    }
    {
        if (value->has_handle) {
            struct json_object *field_json = NULL;

    if (value->handle) {
        field_json = json_object_new_string(value->handle);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "handle", field_json)) goto fail;
        }
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_commit_verifier_params_from_json(const struct json_object *json, waas_commit_verifier_params *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "identityType", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field identityType", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_identity_type_from_string(waas_json_string_value(field_json), &out->identity_type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "authMode", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field authMode", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_auth_mode_from_string(waas_json_string_value(field_json), &out->auth_mode) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "metadata", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field metadata", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    {
        size_t count = (size_t)json_object_object_length((const struct json_object *)field_json);
        out->metadata.keys = count ? (char **)calloc(count, sizeof(*out->metadata.keys)) : NULL;
        out->metadata.values = count ? (char * *)calloc(count, sizeof(*out->metadata.values)) : NULL;
        if (count && (!out->metadata.keys || !out->metadata.values)) {
            waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding map", NULL);
            goto fail;
        }
        out->metadata.count = count;
        size_t idx = 0;
        json_object_object_foreach((struct json_object *)field_json, entry_key, entry) {
            out->metadata.keys[idx] = waas_strdup(entry_key ? entry_key : "");
            if (!out->metadata.keys[idx]) {
                waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding map key", NULL);
                goto fail;
            }

    if (!waas_json_string_value(entry)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->metadata.values[idx] = waas_strdup(waas_json_string_value(entry));
    if (!out->metadata.values[idx]) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            idx++;
        }
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "handle", &field_json);
        if (field_present) {
            out->has_handle = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->handle = waas_strdup(waas_json_string_value(field_json));
    if (!out->handle) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            }
        }
    }
    return 0;
fail:
    waas_commit_verifier_params_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_complete_auth_params_to_json(const waas_complete_auth_params *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_identity_type_to_string(value->identity_type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "identityType", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_auth_mode_to_string(value->auth_mode));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "authMode", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->verifier) {
        field_json = json_object_new_string(value->verifier);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "verifier", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->answer) {
        field_json = json_object_new_string(value->answer);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "answer", field_json)) goto fail;
    }
    {
        if (value->has_lifetime) {
            struct json_object *field_json = NULL;

    field_json = json_object_new_uint64((uint64_t)value->lifetime);
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "lifetime", field_json)) goto fail;
        }
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_complete_auth_params_from_json(const struct json_object *json, waas_complete_auth_params *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "identityType", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field identityType", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_identity_type_from_string(waas_json_string_value(field_json), &out->identity_type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "authMode", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field authMode", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_auth_mode_from_string(waas_json_string_value(field_json), &out->auth_mode) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "verifier", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field verifier", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->verifier = waas_strdup(waas_json_string_value(field_json));
    if (!out->verifier) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "answer", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field answer", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->answer = waas_strdup(waas_json_string_value(field_json));
    if (!out->answer) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "lifetime", &field_json);
        if (field_present) {
            out->has_lifetime = true;
            if (!waas_json_is_null(field_json)) {

    {
        uint64_t parsed = 0;
        if (!waas_json_get_uint64_exact(field_json, &parsed)) {
            waas_set_error(error, 0, 0, "DecodeError", "expected unsigned integer", NULL);
            goto fail;
        }
        if (parsed > UINT64_C(4294967295)) {
            waas_set_error(error, 0, 0, "DecodeError", "unsigned integer out of range", NULL);
            goto fail;
        }
        out->lifetime = (unsigned int)parsed;
    }
            }
        }
    }
    return 0;
fail:
    waas_complete_auth_params_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_create_wallet_params_to_json(const waas_create_wallet_params *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_wallet_type_to_string(value->wallet_type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "walletType", field_json)) goto fail;
    }
    {
        if (value->has_wallet_index) {
            struct json_object *field_json = NULL;

    field_json = json_object_new_uint64((uint64_t)value->wallet_index);
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "walletIndex", field_json)) goto fail;
        }
    }
    {
        if (value->has_use_guard) {
            struct json_object *field_json = NULL;

    field_json = json_object_new_boolean(value->use_guard);
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "useGuard", field_json)) goto fail;
        }
    }
    {
        if (value->has_comment) {
            struct json_object *field_json = NULL;

    if (value->comment) {
        field_json = json_object_new_string(value->comment);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "comment", field_json)) goto fail;
        }
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_create_wallet_params_from_json(const struct json_object *json, waas_create_wallet_params *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "walletType", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field walletType", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_wallet_type_from_string(waas_json_string_value(field_json), &out->wallet_type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "walletIndex", &field_json);
        if (field_present) {
            out->has_wallet_index = true;
            if (!waas_json_is_null(field_json)) {

    {
        uint64_t parsed = 0;
        if (!waas_json_get_uint64_exact(field_json, &parsed)) {
            waas_set_error(error, 0, 0, "DecodeError", "expected unsigned integer", NULL);
            goto fail;
        }
        if (parsed > UINT64_C(255)) {
            waas_set_error(error, 0, 0, "DecodeError", "unsigned integer out of range", NULL);
            goto fail;
        }
        out->wallet_index = (uint8_t)parsed;
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "useGuard", &field_json);
        if (field_present) {
            out->has_use_guard = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_is_bool(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected boolean", NULL);
        goto fail;
    }
    out->use_guard = waas_json_is_true(field_json);
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "comment", &field_json);
        if (field_present) {
            out->has_comment = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->comment = waas_strdup(waas_json_string_value(field_json));
    if (!out->comment) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            }
        }
    }
    return 0;
fail:
    waas_create_wallet_params_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_use_wallet_params_to_json(const waas_use_wallet_params *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_wallet_type_to_string(value->wallet_type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "walletType", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_uint64((uint64_t)value->wallet_index);
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "walletIndex", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_use_wallet_params_from_json(const struct json_object *json, waas_use_wallet_params *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "walletType", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field walletType", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_wallet_type_from_string(waas_json_string_value(field_json), &out->wallet_type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "walletIndex", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field walletIndex", NULL);
            goto fail;
        }

    {
        uint64_t parsed = 0;
        if (!waas_json_get_uint64_exact(field_json, &parsed)) {
            waas_set_error(error, 0, 0, "DecodeError", "expected unsigned integer", NULL);
            goto fail;
        }
        if (parsed > UINT64_C(255)) {
            waas_set_error(error, 0, 0, "DecodeError", "unsigned integer out of range", NULL);
            goto fail;
        }
        out->wallet_index = (uint8_t)parsed;
    }
    }
    return 0;
fail:
    waas_use_wallet_params_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_sign_message_params_to_json(const waas_sign_message_params *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->network) {
        field_json = json_object_new_string(value->network);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "network", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->wallet) {
        field_json = json_object_new_string(value->wallet);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "wallet", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->message) {
        field_json = json_object_new_string(value->message);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "message", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_sign_message_params_from_json(const struct json_object *json, waas_sign_message_params *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "network", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field network", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->network = waas_strdup(waas_json_string_value(field_json));
    if (!out->network) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "wallet", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field wallet", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->wallet = waas_strdup(waas_json_string_value(field_json));
    if (!out->wallet) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "message", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field message", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->message = waas_strdup(waas_json_string_value(field_json));
    if (!out->message) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_sign_message_params_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_send_transaction_params_to_json(const waas_send_transaction_params *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->network) {
        field_json = json_object_new_string(value->network);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "network", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->wallet) {
        field_json = json_object_new_string(value->wallet);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "wallet", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->to) {
        field_json = json_object_new_string(value->to);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "to", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->value) {
        field_json = json_object_new_string(value->value);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "value", field_json)) goto fail;
    }
    {
        if (value->has_data) {
            struct json_object *field_json = NULL;

    if (value->data) {
        field_json = json_object_new_string(value->data);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "data", field_json)) goto fail;
        }
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_transaction_mode_to_string(value->mode));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "mode", field_json)) goto fail;
    }
    {
        if (value->has_fee_ceiling) {
            struct json_object *field_json = NULL;

    if (value->fee_ceiling) {
        field_json = json_object_new_string(value->fee_ceiling);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "feeCeiling", field_json)) goto fail;
        }
    }
    {
        if (value->has_nonce) {
            struct json_object *field_json = NULL;

    if (value->nonce) {
        field_json = json_object_new_string(value->nonce);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "nonce", field_json)) goto fail;
        }
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_send_transaction_params_from_json(const struct json_object *json, waas_send_transaction_params *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "network", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field network", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->network = waas_strdup(waas_json_string_value(field_json));
    if (!out->network) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "wallet", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field wallet", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->wallet = waas_strdup(waas_json_string_value(field_json));
    if (!out->wallet) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "to", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field to", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->to = waas_strdup(waas_json_string_value(field_json));
    if (!out->to) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "value", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field value", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->value = waas_strdup(waas_json_string_value(field_json));
    if (!out->value) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "data", &field_json);
        if (field_present) {
            out->has_data = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->data = waas_strdup(waas_json_string_value(field_json));
    if (!out->data) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "mode", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field mode", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_transaction_mode_from_string(waas_json_string_value(field_json), &out->mode) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "feeCeiling", &field_json);
        if (field_present) {
            out->has_fee_ceiling = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->fee_ceiling = waas_strdup(waas_json_string_value(field_json));
    if (!out->fee_ceiling) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "nonce", &field_json);
        if (field_present) {
            out->has_nonce = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->nonce = waas_strdup(waas_json_string_value(field_json));
    if (!out->nonce) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            }
        }
    }
    return 0;
fail:
    waas_send_transaction_params_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_wallet_to_json(const waas_wallet *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_wallet_type_to_string(value->type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "type", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->address) {
        field_json = json_object_new_string(value->address);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "address", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_uint64((uint64_t)value->index);
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "index", field_json)) goto fail;
    }
    {
        if (value->has_comment) {
            struct json_object *field_json = NULL;

    if (value->comment) {
        field_json = json_object_new_string(value->comment);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "comment", field_json)) goto fail;
        }
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_wallet_from_json(const struct json_object *json, waas_wallet *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "type", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field type", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_wallet_type_from_string(waas_json_string_value(field_json), &out->type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "address", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field address", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->address = waas_strdup(waas_json_string_value(field_json));
    if (!out->address) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "index", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field index", NULL);
            goto fail;
        }

    {
        uint64_t parsed = 0;
        if (!waas_json_get_uint64_exact(field_json, &parsed)) {
            waas_set_error(error, 0, 0, "DecodeError", "expected unsigned integer", NULL);
            goto fail;
        }
        if (parsed > UINT64_C(255)) {
            waas_set_error(error, 0, 0, "DecodeError", "unsigned integer out of range", NULL);
            goto fail;
        }
        out->index = (uint8_t)parsed;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "comment", &field_json);
        if (field_present) {
            out->has_comment = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->comment = waas_strdup(waas_json_string_value(field_json));
    if (!out->comment) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            }
        }
    }
    return 0;
fail:
    waas_wallet_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_transaction_response_to_json(const waas_transaction_response *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->tx_hash) {
        field_json = json_object_new_string(value->tx_hash);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "txHash", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_transaction_response_from_json(const struct json_object *json, waas_transaction_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "txHash", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field txHash", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->tx_hash = waas_strdup(waas_json_string_value(field_json));
    if (!out->tx_hash) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_transaction_response_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_credential_data_to_json(const waas_credential_data *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->scope) {
        field_json = json_object_new_string(value->scope);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "scope", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->credential ? waas_key_to_json(value->credential) : NULL;
    if (value->credential && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "credential", field_json)) goto fail;
    }
    {
        if (value->has_signer) {
            struct json_object *field_json = NULL;

    field_json = value->signer ? waas_key_to_json(value->signer) : NULL;
    if (value->signer && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "signer", field_json)) goto fail;
        }
    }
    {
        if (value->has_identity) {
            struct json_object *field_json = NULL;

    field_json = value->identity ? waas_identity_to_json(value->identity) : NULL;
    if (value->identity && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "identity", field_json)) goto fail;
        }
    }
    {
        if (value->has_auth_key) {
            struct json_object *field_json = NULL;
{
        struct json_object *array_json = json_object_new_array();
        if (!array_json) goto fail;
        for (size_t i = 0; i < value->auth_key.len; ++i) {
            struct json_object *entry_json = json_object_new_uint64((uint64_t)value->auth_key.data[i]);
            if (!entry_json) {
                waas_json_release(array_json);
                goto fail;
            }
            if (!waas_json_add_array_item(array_json, entry_json)) {
                waas_json_release(array_json);
                goto fail;
            }
        }
        field_json = array_json;
    }
            if (!waas_json_add_object_member(root, "authKey", field_json)) goto fail;
        }
    }
    {
        if (value->has_access_token) {
            struct json_object *field_json = NULL;

    field_json = value->access_token ? waas_access_token_to_json(value->access_token) : NULL;
    if (value->access_token && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "accessToken", field_json)) goto fail;
        }
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->created_at.value ? json_object_new_string(value->created_at.value) : NULL;
    if (value->created_at.value && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "createdAt", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->expires_at.value ? json_object_new_string(value->expires_at.value) : NULL;
    if (value->expires_at.value && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "expiresAt", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_credential_data_from_json(const struct json_object *json, waas_credential_data *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "scope", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field scope", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->scope = waas_strdup(waas_json_string_value(field_json));
    if (!out->scope) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "credential", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field credential", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->credential = (waas_key *)calloc(1, sizeof(*out->credential));
    if (!out->credential) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_key_from_json(field_json, out->credential, error) != 0) {
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "signer", &field_json);
        if (field_present) {
            out->has_signer = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->signer = (waas_key *)calloc(1, sizeof(*out->signer));
    if (!out->signer) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_key_from_json(field_json, out->signer, error) != 0) {
        goto fail;
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "identity", &field_json);
        if (field_present) {
            out->has_identity = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->identity = (waas_identity *)calloc(1, sizeof(*out->identity));
    if (!out->identity) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_identity_from_json(field_json, out->identity, error) != 0) {
        goto fail;
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "authKey", &field_json);
        if (field_present) {
            out->has_auth_key = true;
            if (!waas_json_is_null(field_json)) {
if (!waas_json_is_array(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON array", NULL);
        goto fail;
    }
    {
        size_t count = (size_t)waas_json_array_size(field_json);
        out->auth_key.data = count ? (uint8_t *)calloc(count, sizeof(*out->auth_key.data)) : NULL;
        if (count && !out->auth_key.data) {
            waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding byte array", NULL);
            goto fail;
        }
        out->auth_key.len = count;
        for (size_t idx = 0; idx < count; ++idx) {
            struct json_object *entry = waas_json_array_item(field_json, (int)idx);
            uint64_t entry_value = 0;
            if (!waas_json_get_uint64_exact(entry, &entry_value) || entry_value > 255) {
                waas_set_error(error, 0, 0, "DecodeError", "expected byte array element", NULL);
                goto fail;
            }
            out->auth_key.data[idx] = (uint8_t)entry_value;
        }
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "accessToken", &field_json);
        if (field_present) {
            out->has_access_token = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->access_token = (waas_access_token *)calloc(1, sizeof(*out->access_token));
    if (!out->access_token) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_access_token_from_json(field_json, out->access_token, error) != 0) {
        goto fail;
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "createdAt", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field createdAt", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_timestamp_set_string(&out->created_at, waas_json_string_value(field_json)) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected timestamp string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "expiresAt", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field expiresAt", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_timestamp_set_string(&out->expires_at, waas_json_string_value(field_json)) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected timestamp string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_credential_data_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_wallet_binding_data_to_json(const waas_wallet_binding_data *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_wallet_type_to_string(value->wallet_type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "walletType", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->wallet_address) {
        field_json = json_object_new_string(value->wallet_address);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "walletAddress", field_json)) goto fail;
    }
    {
        if (value->has_device_key) {
            struct json_object *field_json = NULL;
{
        struct json_object *array_json = json_object_new_array();
        if (!array_json) goto fail;
        for (size_t i = 0; i < value->device_key.len; ++i) {
            struct json_object *entry_json = json_object_new_uint64((uint64_t)value->device_key.data[i]);
            if (!entry_json) {
                waas_json_release(array_json);
                goto fail;
            }
            if (!waas_json_add_array_item(array_json, entry_json)) {
                waas_json_release(array_json);
                goto fail;
            }
        }
        field_json = array_json;
    }
            if (!waas_json_add_object_member(root, "deviceKey", field_json)) goto fail;
        }
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_boolean(value->active);
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "active", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_wallet_binding_data_from_json(const struct json_object *json, waas_wallet_binding_data *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "walletType", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field walletType", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_wallet_type_from_string(waas_json_string_value(field_json), &out->wallet_type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "walletAddress", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field walletAddress", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->wallet_address = waas_strdup(waas_json_string_value(field_json));
    if (!out->wallet_address) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "deviceKey", &field_json);
        if (field_present) {
            out->has_device_key = true;
            if (!waas_json_is_null(field_json)) {
if (!waas_json_is_array(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON array", NULL);
        goto fail;
    }
    {
        size_t count = (size_t)waas_json_array_size(field_json);
        out->device_key.data = count ? (uint8_t *)calloc(count, sizeof(*out->device_key.data)) : NULL;
        if (count && !out->device_key.data) {
            waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding byte array", NULL);
            goto fail;
        }
        out->device_key.len = count;
        for (size_t idx = 0; idx < count; ++idx) {
            struct json_object *entry = waas_json_array_item(field_json, (int)idx);
            uint64_t entry_value = 0;
            if (!waas_json_get_uint64_exact(entry, &entry_value) || entry_value > 255) {
                waas_set_error(error, 0, 0, "DecodeError", "expected byte array element", NULL);
                goto fail;
            }
            out->device_key.data[idx] = (uint8_t)entry_value;
        }
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "active", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field active", NULL);
            goto fail;
        }

    if (!waas_json_is_bool(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected boolean", NULL);
        goto fail;
    }
    out->active = waas_json_is_true(field_json);
    }
    return 0;
fail:
    waas_wallet_binding_data_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_eoaw_allet_data_to_json(const waas_eoaw_allet_data *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_wallet_type_to_string(value->type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "type", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;
{
        struct json_object *array_json = json_object_new_array();
        if (!array_json) goto fail;
        for (size_t i = 0; i < value->private_key.len; ++i) {
            struct json_object *entry_json = json_object_new_uint64((uint64_t)value->private_key.data[i]);
            if (!entry_json) {
                waas_json_release(array_json);
                goto fail;
            }
            if (!waas_json_add_array_item(array_json, entry_json)) {
                waas_json_release(array_json);
                goto fail;
            }
        }
        field_json = array_json;
    }
            if (!waas_json_add_object_member(root, "privateKey", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_key_origin_to_string(value->origin));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "origin", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_eoaw_allet_data_from_json(const struct json_object *json, waas_eoaw_allet_data *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "type", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field type", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_wallet_type_from_string(waas_json_string_value(field_json), &out->type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "privateKey", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field privateKey", NULL);
            goto fail;
        }
if (!waas_json_is_array(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON array", NULL);
        goto fail;
    }
    {
        size_t count = (size_t)waas_json_array_size(field_json);
        out->private_key.data = count ? (uint8_t *)calloc(count, sizeof(*out->private_key.data)) : NULL;
        if (count && !out->private_key.data) {
            waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding byte array", NULL);
            goto fail;
        }
        out->private_key.len = count;
        for (size_t idx = 0; idx < count; ++idx) {
            struct json_object *entry = waas_json_array_item(field_json, (int)idx);
            uint64_t entry_value = 0;
            if (!waas_json_get_uint64_exact(entry, &entry_value) || entry_value > 255) {
                waas_set_error(error, 0, 0, "DecodeError", "expected byte array element", NULL);
                goto fail;
            }
            out->private_key.data[idx] = (uint8_t)entry_value;
        }
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "origin", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field origin", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_key_origin_from_string(waas_json_string_value(field_json), &out->origin) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_eoaw_allet_data_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_smart_wallet_data_to_json(const waas_smart_wallet_data *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_wallet_type_to_string(value->type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "type", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->deploy_hash) {
        field_json = json_object_new_string(value->deploy_hash);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "deployHash", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_smart_wallet_data_from_json(const struct json_object *json, waas_smart_wallet_data *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "type", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field type", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_wallet_type_from_string(waas_json_string_value(field_json), &out->type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "deployHash", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field deployHash", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->deploy_hash = waas_strdup(waas_json_string_value(field_json));
    if (!out->deploy_hash) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_smart_wallet_data_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_account_data_to_json(const waas_account_data *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->scope) {
        field_json = json_object_new_string(value->scope);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "scope", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->signer ? waas_key_to_json(value->signer) : NULL;
    if (value->signer && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "signer", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->wallet ? waas_wallet_to_json(value->wallet) : NULL;
    if (value->wallet && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "wallet", field_json)) goto fail;
    }
    {
        if (value->has_identity) {
            struct json_object *field_json = NULL;

    field_json = value->identity ? waas_identity_to_json(value->identity) : NULL;
    if (value->identity && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "identity", field_json)) goto fail;
        }
    }
    {
        if (value->has_email) {
            struct json_object *field_json = NULL;

    if (value->email) {
        field_json = json_object_new_string(value->email);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "email", field_json)) goto fail;
        }
    }
    {
        if (value->has_phone) {
            struct json_object *field_json = NULL;

    if (value->phone) {
        field_json = json_object_new_string(value->phone);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "phone", field_json)) goto fail;
        }
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_account_data_from_json(const struct json_object *json, waas_account_data *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "scope", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field scope", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->scope = waas_strdup(waas_json_string_value(field_json));
    if (!out->scope) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "signer", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field signer", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->signer = (waas_key *)calloc(1, sizeof(*out->signer));
    if (!out->signer) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_key_from_json(field_json, out->signer, error) != 0) {
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "wallet", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field wallet", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->wallet = (waas_wallet *)calloc(1, sizeof(*out->wallet));
    if (!out->wallet) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_wallet_from_json(field_json, out->wallet, error) != 0) {
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "identity", &field_json);
        if (field_present) {
            out->has_identity = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->identity = (waas_identity *)calloc(1, sizeof(*out->identity));
    if (!out->identity) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_identity_from_json(field_json, out->identity, error) != 0) {
        goto fail;
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "email", &field_json);
        if (field_present) {
            out->has_email = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->email = waas_strdup(waas_json_string_value(field_json));
    if (!out->email) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            }
        }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "phone", &field_json);
        if (field_present) {
            out->has_phone = true;
            if (!waas_json_is_null(field_json)) {

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->phone = waas_strdup(waas_json_string_value(field_json));
    if (!out->phone) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
            }
        }
    }
    return 0;
fail:
    waas_account_data_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_access_token_to_json(const waas_access_token *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->jwt) {
        field_json = json_object_new_string(value->jwt);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "jwt", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->expires_at.value ? json_object_new_string(value->expires_at.value) : NULL;
    if (value->expires_at.value && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "expiresAt", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_access_token_from_json(const struct json_object *json, waas_access_token *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "jwt", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field jwt", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->jwt = waas_strdup(waas_json_string_value(field_json));
    if (!out->jwt) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "expiresAt", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field expiresAt", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_timestamp_set_string(&out->expires_at, waas_json_string_value(field_json)) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected timestamp string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_access_token_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_session_data_to_json(const waas_session_data *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->scope) {
        field_json = json_object_new_string(value->scope);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "scope", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->wallet) {
        field_json = json_object_new_string(value->wallet);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "wallet", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_session_type_to_string(value->type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "type", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_string(waas_key_type_to_string(value->key_type));
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "keyType", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->private_key) {
        field_json = json_object_new_string(value->private_key);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "privateKey", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->created_at.value ? json_object_new_string(value->created_at.value) : NULL;
    if (value->created_at.value && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "createdAt", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->expires_at.value ? json_object_new_string(value->expires_at.value) : NULL;
    if (value->expires_at.value && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "expiresAt", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_session_data_from_json(const struct json_object *json, waas_session_data *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "scope", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field scope", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->scope = waas_strdup(waas_json_string_value(field_json));
    if (!out->scope) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "wallet", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field wallet", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->wallet = waas_strdup(waas_json_string_value(field_json));
    if (!out->wallet) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "type", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field type", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_session_type_from_string(waas_json_string_value(field_json), &out->type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "keyType", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field keyType", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_key_type_from_string(waas_json_string_value(field_json), &out->key_type) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected enum string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "privateKey", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field privateKey", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->private_key = waas_strdup(waas_json_string_value(field_json));
    if (!out->private_key) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "createdAt", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field createdAt", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_timestamp_set_string(&out->created_at, waas_json_string_value(field_json)) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected timestamp string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "expiresAt", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field expiresAt", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_timestamp_set_string(&out->expires_at, waas_json_string_value(field_json)) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected timestamp string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_session_data_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_credential_info_to_json(const waas_credential_info *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->credential_id) {
        field_json = json_object_new_string(value->credential_id);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "credentialId", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = value->expires_at.value ? json_object_new_string(value->expires_at.value) : NULL;
    if (value->expires_at.value && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "expiresAt", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    field_json = json_object_new_boolean(value->is_caller);
    if (!field_json) goto fail;
            if (!waas_json_add_object_member(root, "isCaller", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_credential_info_from_json(const struct json_object *json, waas_credential_info *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "credentialId", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field credentialId", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->credential_id = waas_strdup(waas_json_string_value(field_json));
    if (!out->credential_id) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "expiresAt", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field expiresAt", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json) || waas_timestamp_set_string(&out->expires_at, waas_json_string_value(field_json)) != 0) {
        waas_set_error(error, 0, 0, "DecodeError", "expected timestamp string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "isCaller", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field isCaller", NULL);
            goto fail;
        }

    if (!waas_json_is_bool(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected boolean", NULL);
        goto fail;
    }
    out->is_caller = waas_json_is_true(field_json);
    }
    return 0;
fail:
    waas_credential_info_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_list_access_params_to_json(const waas_list_access_params *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->wallet_address) {
        field_json = json_object_new_string(value->wallet_address);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "walletAddress", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_list_access_params_from_json(const struct json_object *json, waas_list_access_params *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "walletAddress", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field walletAddress", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->wallet_address = waas_strdup(waas_json_string_value(field_json));
    if (!out->wallet_address) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_list_access_params_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_list_access_result_to_json(const waas_list_access_result *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;
{
        struct json_object *array_json = json_object_new_array();
        if (!array_json) goto fail;
        for (size_t i = 0; i < value->credentials.count; ++i) {
            struct json_object *entry_json = NULL;

    entry_json = value->credentials.items[i] ? waas_credential_info_to_json(value->credentials.items[i]) : NULL;
    if (value->credentials.items[i] && !entry_json) goto fail;
            if (!waas_json_add_array_item(array_json, entry_json)) {
                waas_json_release(array_json);
                goto fail;
            }
        }
        field_json = array_json;
    }
            if (!waas_json_add_object_member(root, "credentials", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_list_access_result_from_json(const struct json_object *json, waas_list_access_result *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "credentials", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field credentials", NULL);
            goto fail;
        }
if (!waas_json_is_array(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON array", NULL);
        goto fail;
    }
    {
        size_t count = (size_t)waas_json_array_size(field_json);
        out->credentials.items = count ? (waas_credential_info * *)calloc(count, sizeof(*out->credentials.items)) : NULL;
        if (count && !out->credentials.items) {
            waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding array", NULL);
            goto fail;
        }
        out->credentials.count = count;
        for (size_t idx = 0; idx < count; ++idx) {
            struct json_object *entry = waas_json_array_item(field_json, (int)idx);

    if (!waas_json_is_object(entry)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->credentials.items[idx] = (waas_credential_info *)calloc(1, sizeof(*out->credentials.items[idx]));
    if (!out->credentials.items[idx]) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_credential_info_from_json(entry, out->credentials.items[idx], error) != 0) {
        goto fail;
    }
        }
    }
    }
    return 0;
fail:
    waas_list_access_result_free(out);
    return -1;
}
static WAAS_JSON_UNUSED struct json_object *waas_revoke_access_params_to_json(const waas_revoke_access_params *value) {
    if (!value) return NULL;
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    if (value->target_credential_id) {
        field_json = json_object_new_string(value->target_credential_id);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "targetCredentialId", field_json)) goto fail;
    }
    {
            struct json_object *field_json = NULL;

    if (value->wallet_address) {
        field_json = json_object_new_string(value->wallet_address);
        if (!field_json) goto fail;
    } else {
        field_json = NULL;
    }
            if (!waas_json_add_object_member(root, "walletAddress", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static WAAS_JSON_UNUSED int waas_revoke_access_params_from_json(const struct json_object *json, waas_revoke_access_params *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || waas_json_is_null(json)) return 0;
    if (!waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "targetCredentialId", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field targetCredentialId", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->target_credential_id = waas_strdup(waas_json_string_value(field_json));
    if (!out->target_credential_id) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "walletAddress", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field walletAddress", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->wallet_address = waas_strdup(waas_json_string_value(field_json));
    if (!out->wallet_address) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_revoke_access_params_free(out);
    return -1;
}

static struct json_object *waas_wallet_commit_verifier_request_to_json(const waas_wallet_commit_verifier_request *value) {
    if (!value) return json_object_new_object();
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = value->params ? waas_commit_verifier_params_to_json(value->params) : NULL;
    if (value->params && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "params", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static int waas_wallet_commit_verifier_response_from_json(const struct json_object *json, waas_wallet_commit_verifier_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || !waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "verifier", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field verifier", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->verifier = waas_strdup(waas_json_string_value(field_json));
    if (!out->verifier) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "loginHint", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field loginHint", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->login_hint = waas_strdup(waas_json_string_value(field_json));
    if (!out->login_hint) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "challenge", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field challenge", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->challenge = waas_strdup(waas_json_string_value(field_json));
    if (!out->challenge) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_wallet_commit_verifier_response_free(out);
    return -1;
}

int waas_wallet_commit_verifier_prepare_request(
    const waas_wallet_commit_verifier_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
) {
    int rc = -1;
    struct json_object *request_json = NULL;
    char *request_body = NULL;
    waas_prepared_request prepared;
    waas_prepared_request_init(&prepared);

    if (!request || !prepared_request) {
        waas_set_error(error, 0, 0, "ClientError", "request and prepared_request must be non-NULL", NULL);
        goto fail;
    }

    request_json = waas_wallet_commit_verifier_request_to_json(request);
    if (!request_json) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to encode request JSON", NULL);
        goto fail;
    }

    request_body = waas_json_serialize(request_json);
    if (!request_body) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to print request JSON", NULL);
        goto fail;
    }

    prepared.http_method = waas_strdup("POST");
    prepared.path = waas_strdup("/rpc/Wallet/CommitVerifier");
    prepared.content_type = waas_strdup("application/json");
    if (!prepared.http_method || !prepared.path || !prepared.content_type) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to allocate prepared request metadata", NULL);
        goto fail;
    }

    prepared.body = request_body;
    prepared.body_len = strlen(request_body);
    request_body = NULL;

    *prepared_request = prepared;
    memset(&prepared, 0, sizeof(prepared));
    rc = 0;

fail:
    waas_prepared_request_free(&prepared);
    free(request_body);
    waas_json_release(request_json);
    return rc;
}

int waas_wallet_commit_verifier_parse_response(
    const waas_http_response *http_response,
    waas_wallet_commit_verifier_response *response,
    waas_error *error
) {
    int rc = -1;
    struct json_object *response_json = NULL;
    waas_wallet_commit_verifier_response parsed_response;
    waas_wallet_commit_verifier_response_init(&parsed_response);

    if (!http_response || !response) {
        waas_set_error(error, 0, 0, "ClientError", "http_response and response must be non-NULL", NULL);
        goto fail;
    }
    if (http_response->status_code < 200 || http_response->status_code >= 300) {
        waas_parse_rpc_error(http_response->body, http_response->status_code, error);
        goto fail;
    }

    response_json = waas_json_parse(http_response->body ? http_response->body : "{}");
    if (!response_json) {
        waas_set_error(error, 0, (int)http_response->status_code, "DecodeError", "failed to parse response JSON", http_response->body);
        goto fail;
    }
    if (waas_wallet_commit_verifier_response_from_json(response_json, &parsed_response, error) != 0) {
        goto fail;
    }

    *response = parsed_response;
    memset(&parsed_response, 0, sizeof(parsed_response));
    rc = 0;

fail:
    waas_wallet_commit_verifier_response_free(&parsed_response);
    waas_json_release(response_json);
    return rc;
}
static struct json_object *waas_wallet_complete_auth_request_to_json(const waas_wallet_complete_auth_request *value) {
    if (!value) return json_object_new_object();
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = value->params ? waas_complete_auth_params_to_json(value->params) : NULL;
    if (value->params && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "params", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static int waas_wallet_complete_auth_response_from_json(const struct json_object *json, waas_wallet_complete_auth_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || !waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "identity", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field identity", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->identity = (waas_identity *)calloc(1, sizeof(*out->identity));
    if (!out->identity) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_identity_from_json(field_json, out->identity, error) != 0) {
        goto fail;
    }
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "wallets", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field wallets", NULL);
            goto fail;
        }
if (!waas_json_is_array(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON array", NULL);
        goto fail;
    }
    {
        size_t count = (size_t)waas_json_array_size(field_json);
        out->wallets.items = count ? (waas_wallet * *)calloc(count, sizeof(*out->wallets.items)) : NULL;
        if (count && !out->wallets.items) {
            waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding array", NULL);
            goto fail;
        }
        out->wallets.count = count;
        for (size_t idx = 0; idx < count; ++idx) {
            struct json_object *entry = waas_json_array_item(field_json, (int)idx);

    if (!waas_json_is_object(entry)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->wallets.items[idx] = (waas_wallet *)calloc(1, sizeof(*out->wallets.items[idx]));
    if (!out->wallets.items[idx]) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_wallet_from_json(entry, out->wallets.items[idx], error) != 0) {
        goto fail;
    }
        }
    }
    }
    return 0;
fail:
    waas_wallet_complete_auth_response_free(out);
    return -1;
}

int waas_wallet_complete_auth_prepare_request(
    const waas_wallet_complete_auth_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
) {
    int rc = -1;
    struct json_object *request_json = NULL;
    char *request_body = NULL;
    waas_prepared_request prepared;
    waas_prepared_request_init(&prepared);

    if (!request || !prepared_request) {
        waas_set_error(error, 0, 0, "ClientError", "request and prepared_request must be non-NULL", NULL);
        goto fail;
    }

    request_json = waas_wallet_complete_auth_request_to_json(request);
    if (!request_json) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to encode request JSON", NULL);
        goto fail;
    }

    request_body = waas_json_serialize(request_json);
    if (!request_body) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to print request JSON", NULL);
        goto fail;
    }

    prepared.http_method = waas_strdup("POST");
    prepared.path = waas_strdup("/rpc/Wallet/CompleteAuth");
    prepared.content_type = waas_strdup("application/json");
    if (!prepared.http_method || !prepared.path || !prepared.content_type) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to allocate prepared request metadata", NULL);
        goto fail;
    }

    prepared.body = request_body;
    prepared.body_len = strlen(request_body);
    request_body = NULL;

    *prepared_request = prepared;
    memset(&prepared, 0, sizeof(prepared));
    rc = 0;

fail:
    waas_prepared_request_free(&prepared);
    free(request_body);
    waas_json_release(request_json);
    return rc;
}

int waas_wallet_complete_auth_parse_response(
    const waas_http_response *http_response,
    waas_wallet_complete_auth_response *response,
    waas_error *error
) {
    int rc = -1;
    struct json_object *response_json = NULL;
    waas_wallet_complete_auth_response parsed_response;
    waas_wallet_complete_auth_response_init(&parsed_response);

    if (!http_response || !response) {
        waas_set_error(error, 0, 0, "ClientError", "http_response and response must be non-NULL", NULL);
        goto fail;
    }
    if (http_response->status_code < 200 || http_response->status_code >= 300) {
        waas_parse_rpc_error(http_response->body, http_response->status_code, error);
        goto fail;
    }

    response_json = waas_json_parse(http_response->body ? http_response->body : "{}");
    if (!response_json) {
        waas_set_error(error, 0, (int)http_response->status_code, "DecodeError", "failed to parse response JSON", http_response->body);
        goto fail;
    }
    if (waas_wallet_complete_auth_response_from_json(response_json, &parsed_response, error) != 0) {
        goto fail;
    }

    *response = parsed_response;
    memset(&parsed_response, 0, sizeof(parsed_response));
    rc = 0;

fail:
    waas_wallet_complete_auth_response_free(&parsed_response);
    waas_json_release(response_json);
    return rc;
}
static struct json_object *waas_wallet_create_wallet_request_to_json(const waas_wallet_create_wallet_request *value) {
    if (!value) return json_object_new_object();
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = value->params ? waas_create_wallet_params_to_json(value->params) : NULL;
    if (value->params && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "params", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static int waas_wallet_create_wallet_response_from_json(const struct json_object *json, waas_wallet_create_wallet_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || !waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "wallet", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field wallet", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->wallet = (waas_wallet *)calloc(1, sizeof(*out->wallet));
    if (!out->wallet) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_wallet_from_json(field_json, out->wallet, error) != 0) {
        goto fail;
    }
    }
    return 0;
fail:
    waas_wallet_create_wallet_response_free(out);
    return -1;
}

int waas_wallet_create_wallet_prepare_request(
    const waas_wallet_create_wallet_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
) {
    int rc = -1;
    struct json_object *request_json = NULL;
    char *request_body = NULL;
    waas_prepared_request prepared;
    waas_prepared_request_init(&prepared);

    if (!request || !prepared_request) {
        waas_set_error(error, 0, 0, "ClientError", "request and prepared_request must be non-NULL", NULL);
        goto fail;
    }

    request_json = waas_wallet_create_wallet_request_to_json(request);
    if (!request_json) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to encode request JSON", NULL);
        goto fail;
    }

    request_body = waas_json_serialize(request_json);
    if (!request_body) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to print request JSON", NULL);
        goto fail;
    }

    prepared.http_method = waas_strdup("POST");
    prepared.path = waas_strdup("/rpc/Wallet/CreateWallet");
    prepared.content_type = waas_strdup("application/json");
    if (!prepared.http_method || !prepared.path || !prepared.content_type) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to allocate prepared request metadata", NULL);
        goto fail;
    }

    prepared.body = request_body;
    prepared.body_len = strlen(request_body);
    request_body = NULL;

    *prepared_request = prepared;
    memset(&prepared, 0, sizeof(prepared));
    rc = 0;

fail:
    waas_prepared_request_free(&prepared);
    free(request_body);
    waas_json_release(request_json);
    return rc;
}

int waas_wallet_create_wallet_parse_response(
    const waas_http_response *http_response,
    waas_wallet_create_wallet_response *response,
    waas_error *error
) {
    int rc = -1;
    struct json_object *response_json = NULL;
    waas_wallet_create_wallet_response parsed_response;
    waas_wallet_create_wallet_response_init(&parsed_response);

    if (!http_response || !response) {
        waas_set_error(error, 0, 0, "ClientError", "http_response and response must be non-NULL", NULL);
        goto fail;
    }
    if (http_response->status_code < 200 || http_response->status_code >= 300) {
        waas_parse_rpc_error(http_response->body, http_response->status_code, error);
        goto fail;
    }

    response_json = waas_json_parse(http_response->body ? http_response->body : "{}");
    if (!response_json) {
        waas_set_error(error, 0, (int)http_response->status_code, "DecodeError", "failed to parse response JSON", http_response->body);
        goto fail;
    }
    if (waas_wallet_create_wallet_response_from_json(response_json, &parsed_response, error) != 0) {
        goto fail;
    }

    *response = parsed_response;
    memset(&parsed_response, 0, sizeof(parsed_response));
    rc = 0;

fail:
    waas_wallet_create_wallet_response_free(&parsed_response);
    waas_json_release(response_json);
    return rc;
}
static struct json_object *waas_wallet_use_wallet_request_to_json(const waas_wallet_use_wallet_request *value) {
    if (!value) return json_object_new_object();
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = value->params ? waas_use_wallet_params_to_json(value->params) : NULL;
    if (value->params && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "params", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static int waas_wallet_use_wallet_response_from_json(const struct json_object *json, waas_wallet_use_wallet_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || !waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "wallet", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field wallet", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->wallet = (waas_wallet *)calloc(1, sizeof(*out->wallet));
    if (!out->wallet) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_wallet_from_json(field_json, out->wallet, error) != 0) {
        goto fail;
    }
    }
    return 0;
fail:
    waas_wallet_use_wallet_response_free(out);
    return -1;
}

int waas_wallet_use_wallet_prepare_request(
    const waas_wallet_use_wallet_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
) {
    int rc = -1;
    struct json_object *request_json = NULL;
    char *request_body = NULL;
    waas_prepared_request prepared;
    waas_prepared_request_init(&prepared);

    if (!request || !prepared_request) {
        waas_set_error(error, 0, 0, "ClientError", "request and prepared_request must be non-NULL", NULL);
        goto fail;
    }

    request_json = waas_wallet_use_wallet_request_to_json(request);
    if (!request_json) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to encode request JSON", NULL);
        goto fail;
    }

    request_body = waas_json_serialize(request_json);
    if (!request_body) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to print request JSON", NULL);
        goto fail;
    }

    prepared.http_method = waas_strdup("POST");
    prepared.path = waas_strdup("/rpc/Wallet/UseWallet");
    prepared.content_type = waas_strdup("application/json");
    if (!prepared.http_method || !prepared.path || !prepared.content_type) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to allocate prepared request metadata", NULL);
        goto fail;
    }

    prepared.body = request_body;
    prepared.body_len = strlen(request_body);
    request_body = NULL;

    *prepared_request = prepared;
    memset(&prepared, 0, sizeof(prepared));
    rc = 0;

fail:
    waas_prepared_request_free(&prepared);
    free(request_body);
    waas_json_release(request_json);
    return rc;
}

int waas_wallet_use_wallet_parse_response(
    const waas_http_response *http_response,
    waas_wallet_use_wallet_response *response,
    waas_error *error
) {
    int rc = -1;
    struct json_object *response_json = NULL;
    waas_wallet_use_wallet_response parsed_response;
    waas_wallet_use_wallet_response_init(&parsed_response);

    if (!http_response || !response) {
        waas_set_error(error, 0, 0, "ClientError", "http_response and response must be non-NULL", NULL);
        goto fail;
    }
    if (http_response->status_code < 200 || http_response->status_code >= 300) {
        waas_parse_rpc_error(http_response->body, http_response->status_code, error);
        goto fail;
    }

    response_json = waas_json_parse(http_response->body ? http_response->body : "{}");
    if (!response_json) {
        waas_set_error(error, 0, (int)http_response->status_code, "DecodeError", "failed to parse response JSON", http_response->body);
        goto fail;
    }
    if (waas_wallet_use_wallet_response_from_json(response_json, &parsed_response, error) != 0) {
        goto fail;
    }

    *response = parsed_response;
    memset(&parsed_response, 0, sizeof(parsed_response));
    rc = 0;

fail:
    waas_wallet_use_wallet_response_free(&parsed_response);
    waas_json_release(response_json);
    return rc;
}
static struct json_object *waas_wallet_sign_message_request_to_json(const waas_wallet_sign_message_request *value) {
    if (!value) return json_object_new_object();
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = value->params ? waas_sign_message_params_to_json(value->params) : NULL;
    if (value->params && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "params", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static int waas_wallet_sign_message_response_from_json(const struct json_object *json, waas_wallet_sign_message_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || !waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "signature", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field signature", NULL);
            goto fail;
        }

    if (!waas_json_string_value(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected string", NULL);
        goto fail;
    }
    out->signature = waas_strdup(waas_json_string_value(field_json));
    if (!out->signature) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding string", NULL);
        goto fail;
    }
    }
    return 0;
fail:
    waas_wallet_sign_message_response_free(out);
    return -1;
}

int waas_wallet_sign_message_prepare_request(
    const waas_wallet_sign_message_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
) {
    int rc = -1;
    struct json_object *request_json = NULL;
    char *request_body = NULL;
    waas_prepared_request prepared;
    waas_prepared_request_init(&prepared);

    if (!request || !prepared_request) {
        waas_set_error(error, 0, 0, "ClientError", "request and prepared_request must be non-NULL", NULL);
        goto fail;
    }

    request_json = waas_wallet_sign_message_request_to_json(request);
    if (!request_json) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to encode request JSON", NULL);
        goto fail;
    }

    request_body = waas_json_serialize(request_json);
    if (!request_body) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to print request JSON", NULL);
        goto fail;
    }

    prepared.http_method = waas_strdup("POST");
    prepared.path = waas_strdup("/rpc/Wallet/SignMessage");
    prepared.content_type = waas_strdup("application/json");
    if (!prepared.http_method || !prepared.path || !prepared.content_type) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to allocate prepared request metadata", NULL);
        goto fail;
    }

    prepared.body = request_body;
    prepared.body_len = strlen(request_body);
    request_body = NULL;

    *prepared_request = prepared;
    memset(&prepared, 0, sizeof(prepared));
    rc = 0;

fail:
    waas_prepared_request_free(&prepared);
    free(request_body);
    waas_json_release(request_json);
    return rc;
}

int waas_wallet_sign_message_parse_response(
    const waas_http_response *http_response,
    waas_wallet_sign_message_response *response,
    waas_error *error
) {
    int rc = -1;
    struct json_object *response_json = NULL;
    waas_wallet_sign_message_response parsed_response;
    waas_wallet_sign_message_response_init(&parsed_response);

    if (!http_response || !response) {
        waas_set_error(error, 0, 0, "ClientError", "http_response and response must be non-NULL", NULL);
        goto fail;
    }
    if (http_response->status_code < 200 || http_response->status_code >= 300) {
        waas_parse_rpc_error(http_response->body, http_response->status_code, error);
        goto fail;
    }

    response_json = waas_json_parse(http_response->body ? http_response->body : "{}");
    if (!response_json) {
        waas_set_error(error, 0, (int)http_response->status_code, "DecodeError", "failed to parse response JSON", http_response->body);
        goto fail;
    }
    if (waas_wallet_sign_message_response_from_json(response_json, &parsed_response, error) != 0) {
        goto fail;
    }

    *response = parsed_response;
    memset(&parsed_response, 0, sizeof(parsed_response));
    rc = 0;

fail:
    waas_wallet_sign_message_response_free(&parsed_response);
    waas_json_release(response_json);
    return rc;
}
static struct json_object *waas_wallet_send_transaction_request_to_json(const waas_wallet_send_transaction_request *value) {
    if (!value) return json_object_new_object();
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = value->params ? waas_send_transaction_params_to_json(value->params) : NULL;
    if (value->params && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "params", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static int waas_wallet_send_transaction_response_from_json(const struct json_object *json, waas_wallet_send_transaction_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || !waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "response", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field response", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->response = (waas_transaction_response *)calloc(1, sizeof(*out->response));
    if (!out->response) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_transaction_response_from_json(field_json, out->response, error) != 0) {
        goto fail;
    }
    }
    return 0;
fail:
    waas_wallet_send_transaction_response_free(out);
    return -1;
}

int waas_wallet_send_transaction_prepare_request(
    const waas_wallet_send_transaction_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
) {
    int rc = -1;
    struct json_object *request_json = NULL;
    char *request_body = NULL;
    waas_prepared_request prepared;
    waas_prepared_request_init(&prepared);

    if (!request || !prepared_request) {
        waas_set_error(error, 0, 0, "ClientError", "request and prepared_request must be non-NULL", NULL);
        goto fail;
    }

    request_json = waas_wallet_send_transaction_request_to_json(request);
    if (!request_json) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to encode request JSON", NULL);
        goto fail;
    }

    request_body = waas_json_serialize(request_json);
    if (!request_body) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to print request JSON", NULL);
        goto fail;
    }

    prepared.http_method = waas_strdup("POST");
    prepared.path = waas_strdup("/rpc/Wallet/SendTransaction");
    prepared.content_type = waas_strdup("application/json");
    if (!prepared.http_method || !prepared.path || !prepared.content_type) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to allocate prepared request metadata", NULL);
        goto fail;
    }

    prepared.body = request_body;
    prepared.body_len = strlen(request_body);
    request_body = NULL;

    *prepared_request = prepared;
    memset(&prepared, 0, sizeof(prepared));
    rc = 0;

fail:
    waas_prepared_request_free(&prepared);
    free(request_body);
    waas_json_release(request_json);
    return rc;
}

int waas_wallet_send_transaction_parse_response(
    const waas_http_response *http_response,
    waas_wallet_send_transaction_response *response,
    waas_error *error
) {
    int rc = -1;
    struct json_object *response_json = NULL;
    waas_wallet_send_transaction_response parsed_response;
    waas_wallet_send_transaction_response_init(&parsed_response);

    if (!http_response || !response) {
        waas_set_error(error, 0, 0, "ClientError", "http_response and response must be non-NULL", NULL);
        goto fail;
    }
    if (http_response->status_code < 200 || http_response->status_code >= 300) {
        waas_parse_rpc_error(http_response->body, http_response->status_code, error);
        goto fail;
    }

    response_json = waas_json_parse(http_response->body ? http_response->body : "{}");
    if (!response_json) {
        waas_set_error(error, 0, (int)http_response->status_code, "DecodeError", "failed to parse response JSON", http_response->body);
        goto fail;
    }
    if (waas_wallet_send_transaction_response_from_json(response_json, &parsed_response, error) != 0) {
        goto fail;
    }

    *response = parsed_response;
    memset(&parsed_response, 0, sizeof(parsed_response));
    rc = 0;

fail:
    waas_wallet_send_transaction_response_free(&parsed_response);
    waas_json_release(response_json);
    return rc;
}
static struct json_object *waas_wallet_list_access_request_to_json(const waas_wallet_list_access_request *value) {
    if (!value) return json_object_new_object();
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = value->params ? waas_list_access_params_to_json(value->params) : NULL;
    if (value->params && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "params", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static int waas_wallet_list_access_response_from_json(const struct json_object *json, waas_wallet_list_access_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || !waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "result", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field result", NULL);
            goto fail;
        }

    if (!waas_json_is_object(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        goto fail;
    }
    out->result = (waas_list_access_result *)calloc(1, sizeof(*out->result));
    if (!out->result) {
        waas_set_error(error, 0, 0, "DecodeError", "out of memory decoding object", NULL);
        goto fail;
    }
    if (waas_list_access_result_from_json(field_json, out->result, error) != 0) {
        goto fail;
    }
    }
    return 0;
fail:
    waas_wallet_list_access_response_free(out);
    return -1;
}

int waas_wallet_list_access_prepare_request(
    const waas_wallet_list_access_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
) {
    int rc = -1;
    struct json_object *request_json = NULL;
    char *request_body = NULL;
    waas_prepared_request prepared;
    waas_prepared_request_init(&prepared);

    if (!request || !prepared_request) {
        waas_set_error(error, 0, 0, "ClientError", "request and prepared_request must be non-NULL", NULL);
        goto fail;
    }

    request_json = waas_wallet_list_access_request_to_json(request);
    if (!request_json) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to encode request JSON", NULL);
        goto fail;
    }

    request_body = waas_json_serialize(request_json);
    if (!request_body) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to print request JSON", NULL);
        goto fail;
    }

    prepared.http_method = waas_strdup("POST");
    prepared.path = waas_strdup("/rpc/Wallet/ListAccess");
    prepared.content_type = waas_strdup("application/json");
    if (!prepared.http_method || !prepared.path || !prepared.content_type) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to allocate prepared request metadata", NULL);
        goto fail;
    }

    prepared.body = request_body;
    prepared.body_len = strlen(request_body);
    request_body = NULL;

    *prepared_request = prepared;
    memset(&prepared, 0, sizeof(prepared));
    rc = 0;

fail:
    waas_prepared_request_free(&prepared);
    free(request_body);
    waas_json_release(request_json);
    return rc;
}

int waas_wallet_list_access_parse_response(
    const waas_http_response *http_response,
    waas_wallet_list_access_response *response,
    waas_error *error
) {
    int rc = -1;
    struct json_object *response_json = NULL;
    waas_wallet_list_access_response parsed_response;
    waas_wallet_list_access_response_init(&parsed_response);

    if (!http_response || !response) {
        waas_set_error(error, 0, 0, "ClientError", "http_response and response must be non-NULL", NULL);
        goto fail;
    }
    if (http_response->status_code < 200 || http_response->status_code >= 300) {
        waas_parse_rpc_error(http_response->body, http_response->status_code, error);
        goto fail;
    }

    response_json = waas_json_parse(http_response->body ? http_response->body : "{}");
    if (!response_json) {
        waas_set_error(error, 0, (int)http_response->status_code, "DecodeError", "failed to parse response JSON", http_response->body);
        goto fail;
    }
    if (waas_wallet_list_access_response_from_json(response_json, &parsed_response, error) != 0) {
        goto fail;
    }

    *response = parsed_response;
    memset(&parsed_response, 0, sizeof(parsed_response));
    rc = 0;

fail:
    waas_wallet_list_access_response_free(&parsed_response);
    waas_json_release(response_json);
    return rc;
}
static struct json_object *waas_wallet_revoke_access_request_to_json(const waas_wallet_revoke_access_request *value) {
    if (!value) return json_object_new_object();
    struct json_object *root = json_object_new_object();
    if (!root) return NULL;
    {
            struct json_object *field_json = NULL;

    field_json = value->params ? waas_revoke_access_params_to_json(value->params) : NULL;
    if (value->params && !field_json) goto fail;
            if (!waas_json_add_object_member(root, "params", field_json)) goto fail;
    }
    return root;
fail:
    waas_json_release(root);
    return NULL;
}

static int waas_wallet_revoke_access_response_from_json(const struct json_object *json, waas_wallet_revoke_access_response *out, waas_error *error) {
    if (!out) {
        waas_set_error(error, 0, 0, "DecodeError", "output pointer is NULL", NULL);
        return -1;
    }
    if (!json || !waas_json_is_object(json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected JSON object", NULL);
        return -1;
    }
    {
        struct json_object *field_json = NULL;
        int field_present = waas_json_object_get(json, "ok", &field_json);
        if (!field_present || waas_json_is_null(field_json)) {
            waas_set_error(error, 0, 0, "DecodeError", "missing required field ok", NULL);
            goto fail;
        }

    if (!waas_json_is_bool(field_json)) {
        waas_set_error(error, 0, 0, "DecodeError", "expected boolean", NULL);
        goto fail;
    }
    out->ok = waas_json_is_true(field_json);
    }
    return 0;
fail:
    waas_wallet_revoke_access_response_free(out);
    return -1;
}

int waas_wallet_revoke_access_prepare_request(
    const waas_wallet_revoke_access_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
) {
    int rc = -1;
    struct json_object *request_json = NULL;
    char *request_body = NULL;
    waas_prepared_request prepared;
    waas_prepared_request_init(&prepared);

    if (!request || !prepared_request) {
        waas_set_error(error, 0, 0, "ClientError", "request and prepared_request must be non-NULL", NULL);
        goto fail;
    }

    request_json = waas_wallet_revoke_access_request_to_json(request);
    if (!request_json) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to encode request JSON", NULL);
        goto fail;
    }

    request_body = waas_json_serialize(request_json);
    if (!request_body) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to print request JSON", NULL);
        goto fail;
    }

    prepared.http_method = waas_strdup("POST");
    prepared.path = waas_strdup("/rpc/Wallet/RevokeAccess");
    prepared.content_type = waas_strdup("application/json");
    if (!prepared.http_method || !prepared.path || !prepared.content_type) {
        waas_set_error(error, 0, 0, "EncodeError", "failed to allocate prepared request metadata", NULL);
        goto fail;
    }

    prepared.body = request_body;
    prepared.body_len = strlen(request_body);
    request_body = NULL;

    *prepared_request = prepared;
    memset(&prepared, 0, sizeof(prepared));
    rc = 0;

fail:
    waas_prepared_request_free(&prepared);
    free(request_body);
    waas_json_release(request_json);
    return rc;
}

int waas_wallet_revoke_access_parse_response(
    const waas_http_response *http_response,
    waas_wallet_revoke_access_response *response,
    waas_error *error
) {
    int rc = -1;
    struct json_object *response_json = NULL;
    waas_wallet_revoke_access_response parsed_response;
    waas_wallet_revoke_access_response_init(&parsed_response);

    if (!http_response || !response) {
        waas_set_error(error, 0, 0, "ClientError", "http_response and response must be non-NULL", NULL);
        goto fail;
    }
    if (http_response->status_code < 200 || http_response->status_code >= 300) {
        waas_parse_rpc_error(http_response->body, http_response->status_code, error);
        goto fail;
    }

    response_json = waas_json_parse(http_response->body ? http_response->body : "{}");
    if (!response_json) {
        waas_set_error(error, 0, (int)http_response->status_code, "DecodeError", "failed to parse response JSON", http_response->body);
        goto fail;
    }
    if (waas_wallet_revoke_access_response_from_json(response_json, &parsed_response, error) != 0) {
        goto fail;
    }

    *response = parsed_response;
    memset(&parsed_response, 0, sizeof(parsed_response));
    rc = 0;

fail:
    waas_wallet_revoke_access_response_free(&parsed_response);
    waas_json_release(response_json);
    return rc;
}

struct waas_wallet_client {
    char *base_url;
    char *bearer_token;
    struct curl_slist *default_headers;
    long timeout_ms;
};

static void waas_wallet_client_reset_config(waas_wallet_client *client) {
    if (!client) return;
    free(client->bearer_token);
    client->bearer_token = NULL;
    if (client->default_headers) {
        curl_slist_free_all(client->default_headers);
        client->default_headers = NULL;
    }
    client->timeout_ms = 10000L;
}

int waas_wallet_client_configure(waas_wallet_client *client, const waas_client_options *options) {
    if (!client) return 0;

    waas_wallet_client_reset_config(client);
    if (!options) return 1;

    if (options->timeout_ms > 0) {
        client->timeout_ms = options->timeout_ms;
    }

    if (options->bearer_token) {
        client->bearer_token = waas_strdup(options->bearer_token);
        if (!client->bearer_token) {
            waas_wallet_client_reset_config(client);
            return 0;
        }
    }

    if (options->headers_count > 0) {
        if (!options->headers) {
            waas_wallet_client_reset_config(client);
            return 0;
        }
        for (size_t i = 0; i < options->headers_count; ++i) {
            if (!options->headers[i]) {
                waas_wallet_client_reset_config(client);
                return 0;
            }
            struct curl_slist *next = curl_slist_append(client->default_headers, options->headers[i]);
            if (!next) {
                waas_wallet_client_reset_config(client);
                return 0;
            }
            client->default_headers = next;
        }
    }

    return 1;
}

waas_wallet_client *waas_wallet_client_create(const char *base_url, const waas_client_options *options) {
    waas_wallet_client *client =
        (waas_wallet_client *)calloc(1, sizeof(*client));
    if (!client) return NULL;
    client->base_url = waas_strdup(base_url ? base_url : "");
    if (!client->base_url) {
        free(client);
        return NULL;
    }
    waas_wallet_client_reset_config(client);
    if (!waas_wallet_client_configure(client, options)) {
        free(client->base_url);
        client->base_url = NULL;
        waas_wallet_client_reset_config(client);
        free(client);
        return NULL;
    }
    return client;
}

void waas_wallet_client_destroy(waas_wallet_client *client) {
    if (!client) return;
    free(client->base_url);
    client->base_url = NULL;
    waas_wallet_client_reset_config(client);
    free(client);
}

int waas_wallet_client_send_prepared_request(
    waas_wallet_client *client,
    const waas_prepared_request *request,
    waas_http_response *response,
    waas_error *error
) {
    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        return -1;
    }

    return waas_http_send_request(
        client->base_url,
        request,
        client->bearer_token,
        client->default_headers,
        client->timeout_ms,
        response,
        error
    );
}
int waas_wallet_commit_verifier(
    waas_wallet_client *client,
    const waas_wallet_commit_verifier_request *request,
    waas_wallet_commit_verifier_response *response,
    waas_error *error
) {
    int rc = -1;
    waas_prepared_request prepared_request;
    waas_http_response http_response;
    waas_prepared_request_init(&prepared_request);
    waas_http_response_init(&http_response);

    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        goto fail;
    }

    if (waas_wallet_commit_verifier_prepare_request(request, &prepared_request, error) != 0) {
        goto fail;
    }
    if (waas_wallet_client_send_prepared_request(client, &prepared_request, &http_response, error) != 0) {
        goto fail;
    }
    if (waas_wallet_commit_verifier_parse_response(&http_response, response, error) != 0) {
        goto fail;
    }
    rc = 0;

fail:
    waas_prepared_request_free(&prepared_request);
    waas_http_response_free(&http_response);
    return rc;
}
int waas_wallet_complete_auth(
    waas_wallet_client *client,
    const waas_wallet_complete_auth_request *request,
    waas_wallet_complete_auth_response *response,
    waas_error *error
) {
    int rc = -1;
    waas_prepared_request prepared_request;
    waas_http_response http_response;
    waas_prepared_request_init(&prepared_request);
    waas_http_response_init(&http_response);

    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        goto fail;
    }

    if (waas_wallet_complete_auth_prepare_request(request, &prepared_request, error) != 0) {
        goto fail;
    }
    if (waas_wallet_client_send_prepared_request(client, &prepared_request, &http_response, error) != 0) {
        goto fail;
    }
    if (waas_wallet_complete_auth_parse_response(&http_response, response, error) != 0) {
        goto fail;
    }
    rc = 0;

fail:
    waas_prepared_request_free(&prepared_request);
    waas_http_response_free(&http_response);
    return rc;
}
int waas_wallet_create_wallet(
    waas_wallet_client *client,
    const waas_wallet_create_wallet_request *request,
    waas_wallet_create_wallet_response *response,
    waas_error *error
) {
    int rc = -1;
    waas_prepared_request prepared_request;
    waas_http_response http_response;
    waas_prepared_request_init(&prepared_request);
    waas_http_response_init(&http_response);

    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        goto fail;
    }

    if (waas_wallet_create_wallet_prepare_request(request, &prepared_request, error) != 0) {
        goto fail;
    }
    if (waas_wallet_client_send_prepared_request(client, &prepared_request, &http_response, error) != 0) {
        goto fail;
    }
    if (waas_wallet_create_wallet_parse_response(&http_response, response, error) != 0) {
        goto fail;
    }
    rc = 0;

fail:
    waas_prepared_request_free(&prepared_request);
    waas_http_response_free(&http_response);
    return rc;
}
int waas_wallet_use_wallet(
    waas_wallet_client *client,
    const waas_wallet_use_wallet_request *request,
    waas_wallet_use_wallet_response *response,
    waas_error *error
) {
    int rc = -1;
    waas_prepared_request prepared_request;
    waas_http_response http_response;
    waas_prepared_request_init(&prepared_request);
    waas_http_response_init(&http_response);

    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        goto fail;
    }

    if (waas_wallet_use_wallet_prepare_request(request, &prepared_request, error) != 0) {
        goto fail;
    }
    if (waas_wallet_client_send_prepared_request(client, &prepared_request, &http_response, error) != 0) {
        goto fail;
    }
    if (waas_wallet_use_wallet_parse_response(&http_response, response, error) != 0) {
        goto fail;
    }
    rc = 0;

fail:
    waas_prepared_request_free(&prepared_request);
    waas_http_response_free(&http_response);
    return rc;
}
int waas_wallet_sign_message(
    waas_wallet_client *client,
    const waas_wallet_sign_message_request *request,
    waas_wallet_sign_message_response *response,
    waas_error *error
) {
    int rc = -1;
    waas_prepared_request prepared_request;
    waas_http_response http_response;
    waas_prepared_request_init(&prepared_request);
    waas_http_response_init(&http_response);

    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        goto fail;
    }

    if (waas_wallet_sign_message_prepare_request(request, &prepared_request, error) != 0) {
        goto fail;
    }
    if (waas_wallet_client_send_prepared_request(client, &prepared_request, &http_response, error) != 0) {
        goto fail;
    }
    if (waas_wallet_sign_message_parse_response(&http_response, response, error) != 0) {
        goto fail;
    }
    rc = 0;

fail:
    waas_prepared_request_free(&prepared_request);
    waas_http_response_free(&http_response);
    return rc;
}
int waas_wallet_send_transaction(
    waas_wallet_client *client,
    const waas_wallet_send_transaction_request *request,
    waas_wallet_send_transaction_response *response,
    waas_error *error
) {
    int rc = -1;
    waas_prepared_request prepared_request;
    waas_http_response http_response;
    waas_prepared_request_init(&prepared_request);
    waas_http_response_init(&http_response);

    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        goto fail;
    }

    if (waas_wallet_send_transaction_prepare_request(request, &prepared_request, error) != 0) {
        goto fail;
    }
    if (waas_wallet_client_send_prepared_request(client, &prepared_request, &http_response, error) != 0) {
        goto fail;
    }
    if (waas_wallet_send_transaction_parse_response(&http_response, response, error) != 0) {
        goto fail;
    }
    rc = 0;

fail:
    waas_prepared_request_free(&prepared_request);
    waas_http_response_free(&http_response);
    return rc;
}
int waas_wallet_list_access(
    waas_wallet_client *client,
    const waas_wallet_list_access_request *request,
    waas_wallet_list_access_response *response,
    waas_error *error
) {
    int rc = -1;
    waas_prepared_request prepared_request;
    waas_http_response http_response;
    waas_prepared_request_init(&prepared_request);
    waas_http_response_init(&http_response);

    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        goto fail;
    }

    if (waas_wallet_list_access_prepare_request(request, &prepared_request, error) != 0) {
        goto fail;
    }
    if (waas_wallet_client_send_prepared_request(client, &prepared_request, &http_response, error) != 0) {
        goto fail;
    }
    if (waas_wallet_list_access_parse_response(&http_response, response, error) != 0) {
        goto fail;
    }
    rc = 0;

fail:
    waas_prepared_request_free(&prepared_request);
    waas_http_response_free(&http_response);
    return rc;
}
int waas_wallet_revoke_access(
    waas_wallet_client *client,
    const waas_wallet_revoke_access_request *request,
    waas_wallet_revoke_access_response *response,
    waas_error *error
) {
    int rc = -1;
    waas_prepared_request prepared_request;
    waas_http_response http_response;
    waas_prepared_request_init(&prepared_request);
    waas_http_response_init(&http_response);

    if (!client || !request || !response) {
        waas_set_error(error, 0, 0, "ClientError", "client, request, and response must be non-NULL", NULL);
        goto fail;
    }

    if (waas_wallet_revoke_access_prepare_request(request, &prepared_request, error) != 0) {
        goto fail;
    }
    if (waas_wallet_client_send_prepared_request(client, &prepared_request, &http_response, error) != 0) {
        goto fail;
    }
    if (waas_wallet_revoke_access_parse_response(&http_response, response, error) != 0) {
        goto fail;
    }
    rc = 0;

fail:
    waas_prepared_request_free(&prepared_request);
    waas_http_response_free(&http_response);
    return rc;
}

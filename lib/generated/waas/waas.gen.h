#ifndef WAAS_WAAS_HEADER_H
#define WAAS_WAAS_HEADER_H

// waas v0.1.0 0980c4988db04d2ff90b5c94df9af7da89aa0487
// --
// Code generated by webrpc-gen@v0.37.1 with github.com/webrpc/gen-c@v0.1.1 generator. DO NOT EDIT.
//
// webrpc-gen -schema=waas.ridl -target=github.com/webrpc/gen-c@v0.1.1 -emit=header -out=./clients/c/waas.gen.h

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#ifdef __cplusplus
extern "C" {
#endif

typedef struct {
    char *digits;
} waas_bigint;

typedef struct {
    char *value;
} waas_timestamp;

typedef struct {
    int code;
    int http_status;
    char *name;
    char *message;
    char *cause;
} waas_error;

typedef struct {
    char *http_method;
    char *path;
    char *body;
    size_t body_len;
    char *content_type;
    char **headers;
    size_t headers_count;
} waas_prepared_request;

typedef struct {
    long status_code;
    char *body;
    size_t body_len;
} waas_http_response;

typedef struct {
    const char *bearer_token;
    const char * const *headers;
    size_t headers_count;
    long timeout_ms;
} waas_client_options;

static inline char *waas_strdup(const char *value) {
    if (!value) return NULL;
    size_t n = strlen(value) + 1;
    char *copy = (char *)malloc(n);
    if (!copy) return NULL;
    memcpy(copy, value, n);
    return copy;
}

static inline void waas_bigint_init(waas_bigint *value) {
    if (!value) return;
    value->digits = NULL;
}

static inline void waas_bigint_free(waas_bigint *value) {
    if (!value) return;
    free(value->digits);
    value->digits = NULL;
}

static inline int waas_bigint_set_string(waas_bigint *value, const char *digits) {
    if (!value || !digits || digits[0] == '\0') return -1;

    size_t start = 0;
    bool negative = false;
    if (digits[0] == '+') return -1;
    if (digits[0] == '-') {
        negative = true;
        start = 1;
    }
    if (digits[start] == '\0') return -1;

    for (size_t i = start; digits[i] != '\0'; ++i) {
        if (digits[i] < '0' || digits[i] > '9') return -1;
    }

    while (digits[start] == '0' && digits[start + 1] != '\0') {
        start++;
    }
    if (digits[start] == '0') negative = false;

    size_t len = strlen(digits + start);
    size_t total = len + (negative ? 2 : 1);
    char *normalized = (char *)malloc(total);
    if (!normalized) return -1;

    if (negative) {
        normalized[0] = '-';
        memcpy(normalized + 1, digits + start, len + 1);
    } else {
        memcpy(normalized, digits + start, len + 1);
    }

    free(value->digits);
    value->digits = normalized;
    return 0;
}

static inline void waas_timestamp_init(waas_timestamp *value) {
    if (!value) return;
    value->value = NULL;
}

static inline void waas_timestamp_free(waas_timestamp *value) {
    if (!value) return;
    free(value->value);
    value->value = NULL;
}

static inline int waas_timestamp_set_string(waas_timestamp *value, const char *timestamp) {
    if (!value) return -1;
    char *copy = waas_strdup(timestamp);
    if (timestamp && !copy) return -1;
    free(value->value);
    value->value = copy;
    return 0;
}

static inline void waas_error_init(waas_error *error) {
    if (!error) return;
    memset(error, 0, sizeof(*error));
}

static inline void waas_error_free(waas_error *error) {
    if (!error) return;
    free(error->name);
    free(error->message);
    free(error->cause);
    memset(error, 0, sizeof(*error));
}

static inline void waas_prepared_request_init(waas_prepared_request *request) {
    if (!request) return;
    memset(request, 0, sizeof(*request));
}

static inline void waas_prepared_request_free(waas_prepared_request *request) {
    if (!request) return;
    free(request->http_method);
    free(request->path);
    free(request->body);
    free(request->content_type);
    if (request->headers) {
        for (size_t i = 0; i < request->headers_count; ++i) {
            free(request->headers[i]);
        }
        free(request->headers);
    }
    memset(request, 0, sizeof(*request));
}

static inline int waas_prepared_request_add_header(waas_prepared_request *request, const char *header) {
    char **next_headers;
    char *copy;

    if (!request || !header) return -1;

    next_headers = (char **)realloc(request->headers, sizeof(*next_headers) * (request->headers_count + 1));
    if (!next_headers) return -1;
    request->headers = next_headers;

    copy = waas_strdup(header);
    if (!copy) {
        return -1;
    }

    request->headers[request->headers_count] = copy;
    request->headers_count += 1;
    return 0;
}

static inline void waas_http_response_init(waas_http_response *response) {
    if (!response) return;
    memset(response, 0, sizeof(*response));
}

static inline void waas_http_response_free(waas_http_response *response) {
    if (!response) return;
    free(response->body);
    response->body = NULL;
    response->body_len = 0;
    response->status_code = 0;
}

static inline void waas_client_options_init(waas_client_options *options) {
    if (!options) return;
    memset(options, 0, sizeof(*options));
    options->timeout_ms = 10000L;
}


typedef struct waas_identity waas_identity;
static inline void waas_identity_init(waas_identity *value);
static inline void waas_identity_free(waas_identity *value);
typedef struct waas_key waas_key;
static inline void waas_key_init(waas_key *value);
static inline void waas_key_free(waas_key *value);
typedef struct waas_commit_verifier_request waas_commit_verifier_request;
static inline void waas_commit_verifier_request_init(waas_commit_verifier_request *value);
static inline void waas_commit_verifier_request_free(waas_commit_verifier_request *value);
typedef struct waas_commit_verifier_response waas_commit_verifier_response;
static inline void waas_commit_verifier_response_init(waas_commit_verifier_response *value);
static inline void waas_commit_verifier_response_free(waas_commit_verifier_response *value);
typedef struct waas_complete_auth_request waas_complete_auth_request;
static inline void waas_complete_auth_request_init(waas_complete_auth_request *value);
static inline void waas_complete_auth_request_free(waas_complete_auth_request *value);
typedef struct waas_complete_auth_response waas_complete_auth_response;
static inline void waas_complete_auth_response_init(waas_complete_auth_response *value);
static inline void waas_complete_auth_response_free(waas_complete_auth_response *value);
typedef struct waas_create_wallet_request waas_create_wallet_request;
static inline void waas_create_wallet_request_init(waas_create_wallet_request *value);
static inline void waas_create_wallet_request_free(waas_create_wallet_request *value);
typedef struct waas_create_wallet_response waas_create_wallet_response;
static inline void waas_create_wallet_response_init(waas_create_wallet_response *value);
static inline void waas_create_wallet_response_free(waas_create_wallet_response *value);
typedef struct waas_use_wallet_request waas_use_wallet_request;
static inline void waas_use_wallet_request_init(waas_use_wallet_request *value);
static inline void waas_use_wallet_request_free(waas_use_wallet_request *value);
typedef struct waas_use_wallet_response waas_use_wallet_response;
static inline void waas_use_wallet_response_init(waas_use_wallet_response *value);
static inline void waas_use_wallet_response_free(waas_use_wallet_response *value);
typedef struct waas_sign_message_request waas_sign_message_request;
static inline void waas_sign_message_request_init(waas_sign_message_request *value);
static inline void waas_sign_message_request_free(waas_sign_message_request *value);
typedef struct waas_sign_message_response waas_sign_message_response;
static inline void waas_sign_message_response_init(waas_sign_message_response *value);
static inline void waas_sign_message_response_free(waas_sign_message_response *value);
typedef struct waas_send_transaction_request waas_send_transaction_request;
static inline void waas_send_transaction_request_init(waas_send_transaction_request *value);
static inline void waas_send_transaction_request_free(waas_send_transaction_request *value);
typedef struct waas_abi_arg waas_abi_arg;
static inline void waas_abi_arg_init(waas_abi_arg *value);
static inline void waas_abi_arg_free(waas_abi_arg *value);
typedef struct waas_call_contract_request waas_call_contract_request;
static inline void waas_call_contract_request_init(waas_call_contract_request *value);
static inline void waas_call_contract_request_free(waas_call_contract_request *value);
typedef struct waas_call_contract_response waas_call_contract_response;
static inline void waas_call_contract_response_init(waas_call_contract_response *value);
static inline void waas_call_contract_response_free(waas_call_contract_response *value);
typedef struct waas_send_transaction_response waas_send_transaction_response;
static inline void waas_send_transaction_response_init(waas_send_transaction_response *value);
static inline void waas_send_transaction_response_free(waas_send_transaction_response *value);
typedef struct waas_list_access_request waas_list_access_request;
static inline void waas_list_access_request_init(waas_list_access_request *value);
static inline void waas_list_access_request_free(waas_list_access_request *value);
typedef struct waas_list_access_response waas_list_access_response;
static inline void waas_list_access_response_init(waas_list_access_response *value);
static inline void waas_list_access_response_free(waas_list_access_response *value);
typedef struct waas_revoke_access_request waas_revoke_access_request;
static inline void waas_revoke_access_request_init(waas_revoke_access_request *value);
static inline void waas_revoke_access_request_free(waas_revoke_access_request *value);
typedef struct waas_revoke_access_response waas_revoke_access_response;
static inline void waas_revoke_access_response_init(waas_revoke_access_response *value);
static inline void waas_revoke_access_response_free(waas_revoke_access_response *value);
typedef struct waas_wallet waas_wallet;
static inline void waas_wallet_init(waas_wallet *value);
static inline void waas_wallet_free(waas_wallet *value);
typedef struct waas_credential_data waas_credential_data;
static inline void waas_credential_data_init(waas_credential_data *value);
static inline void waas_credential_data_free(waas_credential_data *value);
typedef struct waas_wallet_binding_data waas_wallet_binding_data;
static inline void waas_wallet_binding_data_init(waas_wallet_binding_data *value);
static inline void waas_wallet_binding_data_free(waas_wallet_binding_data *value);
typedef struct waas_eoaw_allet_data waas_eoaw_allet_data;
static inline void waas_eoaw_allet_data_init(waas_eoaw_allet_data *value);
static inline void waas_eoaw_allet_data_free(waas_eoaw_allet_data *value);
typedef struct waas_smart_wallet_data waas_smart_wallet_data;
static inline void waas_smart_wallet_data_init(waas_smart_wallet_data *value);
static inline void waas_smart_wallet_data_free(waas_smart_wallet_data *value);
typedef struct waas_account_data waas_account_data;
static inline void waas_account_data_init(waas_account_data *value);
static inline void waas_account_data_free(waas_account_data *value);
typedef struct waas_access_token waas_access_token;
static inline void waas_access_token_init(waas_access_token *value);
static inline void waas_access_token_free(waas_access_token *value);
typedef struct waas_session_data waas_session_data;
static inline void waas_session_data_init(waas_session_data *value);
static inline void waas_session_data_free(waas_session_data *value);
typedef struct waas_credential_info waas_credential_info;
static inline void waas_credential_info_init(waas_credential_info *value);
static inline void waas_credential_info_free(waas_credential_info *value);
typedef enum waas_key_type {
    WAAS_KEY_TYPE_UNKNOWN = 0,
    WAAS_KEY_TYPE_WEB_CRYPTO_SECP256R1,
    WAAS_KEY_TYPE_ETHEREUM_SECP256K1,
} waas_key_type;

static inline const char *waas_key_type_to_string(waas_key_type value) {
    switch (value) {
        case WAAS_KEY_TYPE_UNKNOWN: return "UNKNOWN";
        case WAAS_KEY_TYPE_WEB_CRYPTO_SECP256R1: return "WebCrypto_Secp256r1";
        case WAAS_KEY_TYPE_ETHEREUM_SECP256K1: return "Ethereum_Secp256k1";
        default: return "UNKNOWN";
    }
}

static inline int waas_key_type_from_string(const char *value, waas_key_type *out) {
    if (!out || !value) return -1;
    if (strcmp(value, "WebCrypto_Secp256r1") == 0) {
        *out = WAAS_KEY_TYPE_WEB_CRYPTO_SECP256R1;
        return 0;
    }
    if (strcmp(value, "Ethereum_Secp256k1") == 0) {
        *out = WAAS_KEY_TYPE_ETHEREUM_SECP256K1;
        return 0;
    }
    *out = WAAS_KEY_TYPE_UNKNOWN;
    return -1;
}
typedef enum waas_identity_type {
    WAAS_IDENTITY_TYPE_UNKNOWN = 0,
    WAAS_IDENTITY_TYPE_EMAIL,
    WAAS_IDENTITY_TYPE_OIDC,
} waas_identity_type;

static inline const char *waas_identity_type_to_string(waas_identity_type value) {
    switch (value) {
        case WAAS_IDENTITY_TYPE_UNKNOWN: return "UNKNOWN";
        case WAAS_IDENTITY_TYPE_EMAIL: return "Email";
        case WAAS_IDENTITY_TYPE_OIDC: return "OIDC";
        default: return "UNKNOWN";
    }
}

static inline int waas_identity_type_from_string(const char *value, waas_identity_type *out) {
    if (!out || !value) return -1;
    if (strcmp(value, "Email") == 0) {
        *out = WAAS_IDENTITY_TYPE_EMAIL;
        return 0;
    }
    if (strcmp(value, "OIDC") == 0) {
        *out = WAAS_IDENTITY_TYPE_OIDC;
        return 0;
    }
    *out = WAAS_IDENTITY_TYPE_UNKNOWN;
    return -1;
}
typedef enum waas_auth_mode {
    WAAS_AUTH_MODE_UNKNOWN = 0,
    WAAS_AUTH_MODE_OTP,
    WAAS_AUTH_MODE_IDT_OKEN,
    WAAS_AUTH_MODE_ACCESS_TOKEN,
    WAAS_AUTH_MODE_AUTH_CODE,
    WAAS_AUTH_MODE_AUTH_CODE_PKCE,
} waas_auth_mode;

static inline const char *waas_auth_mode_to_string(waas_auth_mode value) {
    switch (value) {
        case WAAS_AUTH_MODE_UNKNOWN: return "UNKNOWN";
        case WAAS_AUTH_MODE_OTP: return "OTP";
        case WAAS_AUTH_MODE_IDT_OKEN: return "IDToken";
        case WAAS_AUTH_MODE_ACCESS_TOKEN: return "AccessToken";
        case WAAS_AUTH_MODE_AUTH_CODE: return "AuthCode";
        case WAAS_AUTH_MODE_AUTH_CODE_PKCE: return "AuthCodePKCE";
        default: return "UNKNOWN";
    }
}

static inline int waas_auth_mode_from_string(const char *value, waas_auth_mode *out) {
    if (!out || !value) return -1;
    if (strcmp(value, "OTP") == 0) {
        *out = WAAS_AUTH_MODE_OTP;
        return 0;
    }
    if (strcmp(value, "IDToken") == 0) {
        *out = WAAS_AUTH_MODE_IDT_OKEN;
        return 0;
    }
    if (strcmp(value, "AccessToken") == 0) {
        *out = WAAS_AUTH_MODE_ACCESS_TOKEN;
        return 0;
    }
    if (strcmp(value, "AuthCode") == 0) {
        *out = WAAS_AUTH_MODE_AUTH_CODE;
        return 0;
    }
    if (strcmp(value, "AuthCodePKCE") == 0) {
        *out = WAAS_AUTH_MODE_AUTH_CODE_PKCE;
        return 0;
    }
    *out = WAAS_AUTH_MODE_UNKNOWN;
    return -1;
}
typedef enum waas_session_type {
    WAAS_SESSION_TYPE_UNKNOWN = 0,
    WAAS_SESSION_TYPE_DEVICE,
    WAAS_SESSION_TYPE_IMPLICIT,
    WAAS_SESSION_TYPE_EXPLICIT,
} waas_session_type;

static inline const char *waas_session_type_to_string(waas_session_type value) {
    switch (value) {
        case WAAS_SESSION_TYPE_UNKNOWN: return "UNKNOWN";
        case WAAS_SESSION_TYPE_DEVICE: return "Device";
        case WAAS_SESSION_TYPE_IMPLICIT: return "Implicit";
        case WAAS_SESSION_TYPE_EXPLICIT: return "Explicit";
        default: return "UNKNOWN";
    }
}

static inline int waas_session_type_from_string(const char *value, waas_session_type *out) {
    if (!out || !value) return -1;
    if (strcmp(value, "Device") == 0) {
        *out = WAAS_SESSION_TYPE_DEVICE;
        return 0;
    }
    if (strcmp(value, "Implicit") == 0) {
        *out = WAAS_SESSION_TYPE_IMPLICIT;
        return 0;
    }
    if (strcmp(value, "Explicit") == 0) {
        *out = WAAS_SESSION_TYPE_EXPLICIT;
        return 0;
    }
    *out = WAAS_SESSION_TYPE_UNKNOWN;
    return -1;
}
typedef enum waas_wallet_type {
    WAAS_WALLET_TYPE_UNKNOWN = 0,
    WAAS_WALLET_TYPE_ETHEREUM_EOA,
    WAAS_WALLET_TYPE_ETHEREUM_SEQUENCE_V3,
} waas_wallet_type;

static inline const char *waas_wallet_type_to_string(waas_wallet_type value) {
    switch (value) {
        case WAAS_WALLET_TYPE_UNKNOWN: return "UNKNOWN";
        case WAAS_WALLET_TYPE_ETHEREUM_EOA: return "Ethereum_EOA";
        case WAAS_WALLET_TYPE_ETHEREUM_SEQUENCE_V3: return "Ethereum_SequenceV3";
        default: return "UNKNOWN";
    }
}

static inline int waas_wallet_type_from_string(const char *value, waas_wallet_type *out) {
    if (!out || !value) return -1;
    if (strcmp(value, "Ethereum_EOA") == 0) {
        *out = WAAS_WALLET_TYPE_ETHEREUM_EOA;
        return 0;
    }
    if (strcmp(value, "Ethereum_SequenceV3") == 0) {
        *out = WAAS_WALLET_TYPE_ETHEREUM_SEQUENCE_V3;
        return 0;
    }
    *out = WAAS_WALLET_TYPE_UNKNOWN;
    return -1;
}
typedef enum waas_transaction_mode {
    WAAS_TRANSACTION_MODE_UNKNOWN = 0,
    WAAS_TRANSACTION_MODE_NATIVE,
    WAAS_TRANSACTION_MODE_RELAYER,
} waas_transaction_mode;

static inline const char *waas_transaction_mode_to_string(waas_transaction_mode value) {
    switch (value) {
        case WAAS_TRANSACTION_MODE_UNKNOWN: return "UNKNOWN";
        case WAAS_TRANSACTION_MODE_NATIVE: return "Native";
        case WAAS_TRANSACTION_MODE_RELAYER: return "Relayer";
        default: return "UNKNOWN";
    }
}

static inline int waas_transaction_mode_from_string(const char *value, waas_transaction_mode *out) {
    if (!out || !value) return -1;
    if (strcmp(value, "Native") == 0) {
        *out = WAAS_TRANSACTION_MODE_NATIVE;
        return 0;
    }
    if (strcmp(value, "Relayer") == 0) {
        *out = WAAS_TRANSACTION_MODE_RELAYER;
        return 0;
    }
    *out = WAAS_TRANSACTION_MODE_UNKNOWN;
    return -1;
}
typedef enum waas_key_origin {
    WAAS_KEY_ORIGIN_UNKNOWN = 0,
    WAAS_KEY_ORIGIN_ENCLAVE,
    WAAS_KEY_ORIGIN_IMPORTED,
} waas_key_origin;

static inline const char *waas_key_origin_to_string(waas_key_origin value) {
    switch (value) {
        case WAAS_KEY_ORIGIN_UNKNOWN: return "UNKNOWN";
        case WAAS_KEY_ORIGIN_ENCLAVE: return "Enclave";
        case WAAS_KEY_ORIGIN_IMPORTED: return "Imported";
        default: return "UNKNOWN";
    }
}

static inline int waas_key_origin_from_string(const char *value, waas_key_origin *out) {
    if (!out || !value) return -1;
    if (strcmp(value, "Enclave") == 0) {
        *out = WAAS_KEY_ORIGIN_ENCLAVE;
        return 0;
    }
    if (strcmp(value, "Imported") == 0) {
        *out = WAAS_KEY_ORIGIN_IMPORTED;
        return 0;
    }
    *out = WAAS_KEY_ORIGIN_UNKNOWN;
    return -1;
}
struct waas_identity {
    waas_identity_type type;
    char * issuer;
    char * subject;
    char * email;
};

static inline void waas_identity_init(waas_identity *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_identity_free(waas_identity *value) {
    if (!value) return;


    free(value->issuer);
    value->issuer = NULL;

    free(value->subject);
    value->subject = NULL;

    free(value->email);
    value->email = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_key {
    waas_key_type key_type;
    char * address;
};

static inline void waas_key_init(waas_key *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_key_free(waas_key *value) {
    if (!value) return;


    free(value->address);
    value->address = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_commit_verifier_request {
    waas_identity_type identity_type;
    waas_auth_mode auth_mode;
    struct {
    char * *keys;
    char * *values;
    size_t count;
} metadata;
    bool has_handle;
    char * handle;
};

static inline void waas_commit_verifier_request_init(waas_commit_verifier_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_commit_verifier_request_free(waas_commit_verifier_request *value) {
    if (!value) return;



    if (value->metadata.keys || value->metadata.values) {
        for (size_t free_map_idx_0 = 0; free_map_idx_0 < value->metadata.count; ++free_map_idx_0) {

    free(value->metadata.keys[free_map_idx_0]);
    value->metadata.keys[free_map_idx_0] = NULL;

    free(value->metadata.values[free_map_idx_0]);
    value->metadata.values[free_map_idx_0] = NULL;
        }
        free(value->metadata.keys);
        free(value->metadata.values);
        value->metadata.keys = NULL;
        value->metadata.values = NULL;
        value->metadata.count = 0;
    }

    free(value->handle);
    value->handle = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_commit_verifier_response {
    char * verifier;
    bool has_login_hint;
    char * login_hint;
    char * challenge;
};

static inline void waas_commit_verifier_response_init(waas_commit_verifier_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_commit_verifier_response_free(waas_commit_verifier_response *value) {
    if (!value) return;

    free(value->verifier);
    value->verifier = NULL;

    free(value->login_hint);
    value->login_hint = NULL;

    free(value->challenge);
    value->challenge = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_complete_auth_request {
    waas_identity_type identity_type;
    waas_auth_mode auth_mode;
    char * verifier;
    char * answer;
    bool has_lifetime;
    unsigned int lifetime;
};

static inline void waas_complete_auth_request_init(waas_complete_auth_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_complete_auth_request_free(waas_complete_auth_request *value) {
    if (!value) return;



    free(value->verifier);
    value->verifier = NULL;

    free(value->answer);
    value->answer = NULL;

    memset(value, 0, sizeof(*value));
}
struct waas_complete_auth_response {
    waas_identity * identity;
    struct {
    waas_wallet * *items;
    size_t count;
} wallets;
};

static inline void waas_complete_auth_response_init(waas_complete_auth_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_complete_auth_response_free(waas_complete_auth_response *value) {
    if (!value) return;

    if (value->identity) {
        waas_identity_free(value->identity);
        free(value->identity);
        value->identity = NULL;
    }

    if (value->wallets.items) {
        for (size_t free_list_idx_0 = 0; free_list_idx_0 < value->wallets.count; ++free_list_idx_0) {

    if (value->wallets.items[free_list_idx_0]) {
        waas_wallet_free(value->wallets.items[free_list_idx_0]);
        free(value->wallets.items[free_list_idx_0]);
        value->wallets.items[free_list_idx_0] = NULL;
    }
        }
        free(value->wallets.items);
        value->wallets.items = NULL;
        value->wallets.count = 0;
    }
    memset(value, 0, sizeof(*value));
}
struct waas_create_wallet_request {
    waas_wallet_type wallet_type;
    bool has_wallet_index;
    uint8_t wallet_index;
    bool has_use_guard;
    bool use_guard;
    bool has_comment;
    char * comment;
};

static inline void waas_create_wallet_request_init(waas_create_wallet_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_create_wallet_request_free(waas_create_wallet_request *value) {
    if (!value) return;




    free(value->comment);
    value->comment = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_create_wallet_response {
    waas_wallet * wallet;
};

static inline void waas_create_wallet_response_init(waas_create_wallet_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_create_wallet_response_free(waas_create_wallet_response *value) {
    if (!value) return;

    if (value->wallet) {
        waas_wallet_free(value->wallet);
        free(value->wallet);
        value->wallet = NULL;
    }
    memset(value, 0, sizeof(*value));
}
struct waas_use_wallet_request {
    waas_wallet_type wallet_type;
    uint8_t wallet_index;
};

static inline void waas_use_wallet_request_init(waas_use_wallet_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_use_wallet_request_free(waas_use_wallet_request *value) {
    if (!value) return;


    memset(value, 0, sizeof(*value));
}
struct waas_use_wallet_response {
    waas_wallet * wallet;
};

static inline void waas_use_wallet_response_init(waas_use_wallet_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_use_wallet_response_free(waas_use_wallet_response *value) {
    if (!value) return;

    if (value->wallet) {
        waas_wallet_free(value->wallet);
        free(value->wallet);
        value->wallet = NULL;
    }
    memset(value, 0, sizeof(*value));
}
struct waas_sign_message_request {
    char * network;
    char * wallet;
    char * message;
};

static inline void waas_sign_message_request_init(waas_sign_message_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_sign_message_request_free(waas_sign_message_request *value) {
    if (!value) return;

    free(value->network);
    value->network = NULL;

    free(value->wallet);
    value->wallet = NULL;

    free(value->message);
    value->message = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_sign_message_response {
    char * signature;
};

static inline void waas_sign_message_response_init(waas_sign_message_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_sign_message_response_free(waas_sign_message_response *value) {
    if (!value) return;

    free(value->signature);
    value->signature = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_send_transaction_request {
    char * network;
    char * wallet;
    char * to;
    char * value;
    bool has_data;
    char * data;
    waas_transaction_mode mode;
    bool has_fee_ceiling;
    char * fee_ceiling;
    bool has_nonce;
    char * nonce;
};

static inline void waas_send_transaction_request_init(waas_send_transaction_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_send_transaction_request_free(waas_send_transaction_request *value) {
    if (!value) return;

    free(value->network);
    value->network = NULL;

    free(value->wallet);
    value->wallet = NULL;

    free(value->to);
    value->to = NULL;

    free(value->value);
    value->value = NULL;

    free(value->data);
    value->data = NULL;


    free(value->fee_ceiling);
    value->fee_ceiling = NULL;

    free(value->nonce);
    value->nonce = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_abi_arg {
    char * type;
    char * value;
};

static inline void waas_abi_arg_init(waas_abi_arg *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_abi_arg_free(waas_abi_arg *value) {
    if (!value) return;

    free(value->type);
    value->type = NULL;

    free(value->value);
    value->value = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_call_contract_request {
    char * network;
    char * wallet;
    char * contract_address;
    char * method;
    bool has_args;
    struct {
    waas_abi_arg * *items;
    size_t count;
} args;
    bool has_value;
    char * value;
    waas_transaction_mode mode;
    bool has_fee_ceiling;
    char * fee_ceiling;
    bool has_nonce;
    char * nonce;
};

static inline void waas_call_contract_request_init(waas_call_contract_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_call_contract_request_free(waas_call_contract_request *value) {
    if (!value) return;

    free(value->network);
    value->network = NULL;

    free(value->wallet);
    value->wallet = NULL;

    free(value->contract_address);
    value->contract_address = NULL;

    free(value->method);
    value->method = NULL;

    if (value->args.items) {
        for (size_t free_list_idx_0 = 0; free_list_idx_0 < value->args.count; ++free_list_idx_0) {

    if (value->args.items[free_list_idx_0]) {
        waas_abi_arg_free(value->args.items[free_list_idx_0]);
        free(value->args.items[free_list_idx_0]);
        value->args.items[free_list_idx_0] = NULL;
    }
        }
        free(value->args.items);
        value->args.items = NULL;
        value->args.count = 0;
    }

    free(value->value);
    value->value = NULL;


    free(value->fee_ceiling);
    value->fee_ceiling = NULL;

    free(value->nonce);
    value->nonce = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_call_contract_response {
    char * tx_hash;
};

static inline void waas_call_contract_response_init(waas_call_contract_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_call_contract_response_free(waas_call_contract_response *value) {
    if (!value) return;

    free(value->tx_hash);
    value->tx_hash = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_send_transaction_response {
    char * tx_hash;
};

static inline void waas_send_transaction_response_init(waas_send_transaction_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_send_transaction_response_free(waas_send_transaction_response *value) {
    if (!value) return;

    free(value->tx_hash);
    value->tx_hash = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_list_access_request {
    char * wallet_address;
};

static inline void waas_list_access_request_init(waas_list_access_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_list_access_request_free(waas_list_access_request *value) {
    if (!value) return;

    free(value->wallet_address);
    value->wallet_address = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_list_access_response {
    struct {
    waas_credential_info * *items;
    size_t count;
} credentials;
};

static inline void waas_list_access_response_init(waas_list_access_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_list_access_response_free(waas_list_access_response *value) {
    if (!value) return;

    if (value->credentials.items) {
        for (size_t free_list_idx_0 = 0; free_list_idx_0 < value->credentials.count; ++free_list_idx_0) {

    if (value->credentials.items[free_list_idx_0]) {
        waas_credential_info_free(value->credentials.items[free_list_idx_0]);
        free(value->credentials.items[free_list_idx_0]);
        value->credentials.items[free_list_idx_0] = NULL;
    }
        }
        free(value->credentials.items);
        value->credentials.items = NULL;
        value->credentials.count = 0;
    }
    memset(value, 0, sizeof(*value));
}
struct waas_revoke_access_request {
    char * target_credential_id;
    char * wallet_address;
};

static inline void waas_revoke_access_request_init(waas_revoke_access_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_revoke_access_request_free(waas_revoke_access_request *value) {
    if (!value) return;

    free(value->target_credential_id);
    value->target_credential_id = NULL;

    free(value->wallet_address);
    value->wallet_address = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_revoke_access_response {
    bool ok;
};

static inline void waas_revoke_access_response_init(waas_revoke_access_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_revoke_access_response_free(waas_revoke_access_response *value) {
    if (!value) return;

    memset(value, 0, sizeof(*value));
}
struct waas_wallet {
    waas_wallet_type type;
    char * address;
    uint8_t index;
    bool has_comment;
    char * comment;
};

static inline void waas_wallet_init(waas_wallet *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_free(waas_wallet *value) {
    if (!value) return;


    free(value->address);
    value->address = NULL;


    free(value->comment);
    value->comment = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_credential_data {
    char * scope;
    waas_key * credential;
    bool has_signer;
    waas_key * signer;
    bool has_identity;
    waas_identity * identity;
    bool has_auth_key;
    struct {
    uint8_t *data;
    size_t len;
} auth_key;
    bool has_access_token;
    waas_access_token * access_token;
    waas_timestamp created_at;
    waas_timestamp expires_at;
};

static inline void waas_credential_data_init(waas_credential_data *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_credential_data_free(waas_credential_data *value) {
    if (!value) return;

    free(value->scope);
    value->scope = NULL;

    if (value->credential) {
        waas_key_free(value->credential);
        free(value->credential);
        value->credential = NULL;
    }

    if (value->signer) {
        waas_key_free(value->signer);
        free(value->signer);
        value->signer = NULL;
    }

    if (value->identity) {
        waas_identity_free(value->identity);
        free(value->identity);
        value->identity = NULL;
    }

    free(value->auth_key.data);
    value->auth_key.data = NULL;
    value->auth_key.len = 0;

    if (value->access_token) {
        waas_access_token_free(value->access_token);
        free(value->access_token);
        value->access_token = NULL;
    }

    waas_timestamp_free(&value->created_at);

    waas_timestamp_free(&value->expires_at);
    memset(value, 0, sizeof(*value));
}
struct waas_wallet_binding_data {
    waas_wallet_type wallet_type;
    char * wallet_address;
    bool has_device_key;
    struct {
    uint8_t *data;
    size_t len;
} device_key;
    bool active;
};

static inline void waas_wallet_binding_data_init(waas_wallet_binding_data *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_binding_data_free(waas_wallet_binding_data *value) {
    if (!value) return;


    free(value->wallet_address);
    value->wallet_address = NULL;

    free(value->device_key.data);
    value->device_key.data = NULL;
    value->device_key.len = 0;

    memset(value, 0, sizeof(*value));
}
struct waas_eoaw_allet_data {
    waas_wallet_type type;
    struct {
    uint8_t *data;
    size_t len;
} private_key;
    waas_key_origin origin;
};

static inline void waas_eoaw_allet_data_init(waas_eoaw_allet_data *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_eoaw_allet_data_free(waas_eoaw_allet_data *value) {
    if (!value) return;


    free(value->private_key.data);
    value->private_key.data = NULL;
    value->private_key.len = 0;

    memset(value, 0, sizeof(*value));
}
struct waas_smart_wallet_data {
    waas_wallet_type type;
    char * deploy_hash;
};

static inline void waas_smart_wallet_data_init(waas_smart_wallet_data *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_smart_wallet_data_free(waas_smart_wallet_data *value) {
    if (!value) return;


    free(value->deploy_hash);
    value->deploy_hash = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_account_data {
    char * scope;
    waas_key * signer;
    waas_wallet * wallet;
    bool has_identity;
    waas_identity * identity;
    bool has_email;
    char * email;
    bool has_phone;
    char * phone;
};

static inline void waas_account_data_init(waas_account_data *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_account_data_free(waas_account_data *value) {
    if (!value) return;

    free(value->scope);
    value->scope = NULL;

    if (value->signer) {
        waas_key_free(value->signer);
        free(value->signer);
        value->signer = NULL;
    }

    if (value->wallet) {
        waas_wallet_free(value->wallet);
        free(value->wallet);
        value->wallet = NULL;
    }

    if (value->identity) {
        waas_identity_free(value->identity);
        free(value->identity);
        value->identity = NULL;
    }

    free(value->email);
    value->email = NULL;

    free(value->phone);
    value->phone = NULL;
    memset(value, 0, sizeof(*value));
}
struct waas_access_token {
    char * jwt;
    waas_timestamp expires_at;
};

static inline void waas_access_token_init(waas_access_token *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_access_token_free(waas_access_token *value) {
    if (!value) return;

    free(value->jwt);
    value->jwt = NULL;

    waas_timestamp_free(&value->expires_at);
    memset(value, 0, sizeof(*value));
}
struct waas_session_data {
    char * scope;
    char * wallet;
    waas_session_type type;
    waas_key_type key_type;
    char * private_key;
    waas_timestamp created_at;
    waas_timestamp expires_at;
};

static inline void waas_session_data_init(waas_session_data *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_session_data_free(waas_session_data *value) {
    if (!value) return;

    free(value->scope);
    value->scope = NULL;

    free(value->wallet);
    value->wallet = NULL;



    free(value->private_key);
    value->private_key = NULL;

    waas_timestamp_free(&value->created_at);

    waas_timestamp_free(&value->expires_at);
    memset(value, 0, sizeof(*value));
}
struct waas_credential_info {
    char * credential_id;
    waas_timestamp expires_at;
    bool is_caller;
};

static inline void waas_credential_info_init(waas_credential_info *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_credential_info_free(waas_credential_info *value) {
    if (!value) return;

    free(value->credential_id);
    value->credential_id = NULL;

    waas_timestamp_free(&value->expires_at);

    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_commit_verifier_request {
    waas_commit_verifier_request * commit_verifier_request;
} waas_wallet_commit_verifier_request;

static inline void waas_wallet_commit_verifier_request_init(waas_wallet_commit_verifier_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_commit_verifier_request_free(waas_wallet_commit_verifier_request *value) {
    if (!value) return;

    if (value->commit_verifier_request) {
        waas_commit_verifier_request_free(value->commit_verifier_request);
        free(value->commit_verifier_request);
        value->commit_verifier_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_commit_verifier_response {
    waas_commit_verifier_response * commit_verifier_response;
} waas_wallet_commit_verifier_response;

static inline void waas_wallet_commit_verifier_response_init(waas_wallet_commit_verifier_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_commit_verifier_response_free(waas_wallet_commit_verifier_response *value) {
    if (!value) return;

    if (value->commit_verifier_response) {
        waas_commit_verifier_response_free(value->commit_verifier_response);
        free(value->commit_verifier_response);
        value->commit_verifier_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_complete_auth_request {
    waas_complete_auth_request * complete_auth_request;
} waas_wallet_complete_auth_request;

static inline void waas_wallet_complete_auth_request_init(waas_wallet_complete_auth_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_complete_auth_request_free(waas_wallet_complete_auth_request *value) {
    if (!value) return;

    if (value->complete_auth_request) {
        waas_complete_auth_request_free(value->complete_auth_request);
        free(value->complete_auth_request);
        value->complete_auth_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_complete_auth_response {
    waas_complete_auth_response * complete_auth_response;
} waas_wallet_complete_auth_response;

static inline void waas_wallet_complete_auth_response_init(waas_wallet_complete_auth_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_complete_auth_response_free(waas_wallet_complete_auth_response *value) {
    if (!value) return;

    if (value->complete_auth_response) {
        waas_complete_auth_response_free(value->complete_auth_response);
        free(value->complete_auth_response);
        value->complete_auth_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_create_wallet_request {
    waas_create_wallet_request * create_wallet_request;
} waas_wallet_create_wallet_request;

static inline void waas_wallet_create_wallet_request_init(waas_wallet_create_wallet_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_create_wallet_request_free(waas_wallet_create_wallet_request *value) {
    if (!value) return;

    if (value->create_wallet_request) {
        waas_create_wallet_request_free(value->create_wallet_request);
        free(value->create_wallet_request);
        value->create_wallet_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_create_wallet_response {
    waas_create_wallet_response * create_wallet_response;
} waas_wallet_create_wallet_response;

static inline void waas_wallet_create_wallet_response_init(waas_wallet_create_wallet_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_create_wallet_response_free(waas_wallet_create_wallet_response *value) {
    if (!value) return;

    if (value->create_wallet_response) {
        waas_create_wallet_response_free(value->create_wallet_response);
        free(value->create_wallet_response);
        value->create_wallet_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_use_wallet_request {
    waas_use_wallet_request * use_wallet_request;
} waas_wallet_use_wallet_request;

static inline void waas_wallet_use_wallet_request_init(waas_wallet_use_wallet_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_use_wallet_request_free(waas_wallet_use_wallet_request *value) {
    if (!value) return;

    if (value->use_wallet_request) {
        waas_use_wallet_request_free(value->use_wallet_request);
        free(value->use_wallet_request);
        value->use_wallet_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_use_wallet_response {
    waas_use_wallet_response * use_wallet_response;
} waas_wallet_use_wallet_response;

static inline void waas_wallet_use_wallet_response_init(waas_wallet_use_wallet_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_use_wallet_response_free(waas_wallet_use_wallet_response *value) {
    if (!value) return;

    if (value->use_wallet_response) {
        waas_use_wallet_response_free(value->use_wallet_response);
        free(value->use_wallet_response);
        value->use_wallet_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_sign_message_request {
    waas_sign_message_request * sign_message_request;
} waas_wallet_sign_message_request;

static inline void waas_wallet_sign_message_request_init(waas_wallet_sign_message_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_sign_message_request_free(waas_wallet_sign_message_request *value) {
    if (!value) return;

    if (value->sign_message_request) {
        waas_sign_message_request_free(value->sign_message_request);
        free(value->sign_message_request);
        value->sign_message_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_sign_message_response {
    waas_sign_message_response * sign_message_response;
} waas_wallet_sign_message_response;

static inline void waas_wallet_sign_message_response_init(waas_wallet_sign_message_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_sign_message_response_free(waas_wallet_sign_message_response *value) {
    if (!value) return;

    if (value->sign_message_response) {
        waas_sign_message_response_free(value->sign_message_response);
        free(value->sign_message_response);
        value->sign_message_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_send_transaction_request {
    waas_send_transaction_request * send_transaction_request;
} waas_wallet_send_transaction_request;

static inline void waas_wallet_send_transaction_request_init(waas_wallet_send_transaction_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_send_transaction_request_free(waas_wallet_send_transaction_request *value) {
    if (!value) return;

    if (value->send_transaction_request) {
        waas_send_transaction_request_free(value->send_transaction_request);
        free(value->send_transaction_request);
        value->send_transaction_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_send_transaction_response {
    waas_send_transaction_response * send_transaction_response;
} waas_wallet_send_transaction_response;

static inline void waas_wallet_send_transaction_response_init(waas_wallet_send_transaction_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_send_transaction_response_free(waas_wallet_send_transaction_response *value) {
    if (!value) return;

    if (value->send_transaction_response) {
        waas_send_transaction_response_free(value->send_transaction_response);
        free(value->send_transaction_response);
        value->send_transaction_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_call_contract_request {
    waas_call_contract_request * call_contract_request;
} waas_wallet_call_contract_request;

static inline void waas_wallet_call_contract_request_init(waas_wallet_call_contract_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_call_contract_request_free(waas_wallet_call_contract_request *value) {
    if (!value) return;

    if (value->call_contract_request) {
        waas_call_contract_request_free(value->call_contract_request);
        free(value->call_contract_request);
        value->call_contract_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_call_contract_response {
    waas_call_contract_response * call_contract_response;
} waas_wallet_call_contract_response;

static inline void waas_wallet_call_contract_response_init(waas_wallet_call_contract_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_call_contract_response_free(waas_wallet_call_contract_response *value) {
    if (!value) return;

    if (value->call_contract_response) {
        waas_call_contract_response_free(value->call_contract_response);
        free(value->call_contract_response);
        value->call_contract_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_list_access_request {
    waas_list_access_request * list_access_request;
} waas_wallet_list_access_request;

static inline void waas_wallet_list_access_request_init(waas_wallet_list_access_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_list_access_request_free(waas_wallet_list_access_request *value) {
    if (!value) return;

    if (value->list_access_request) {
        waas_list_access_request_free(value->list_access_request);
        free(value->list_access_request);
        value->list_access_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_list_access_response {
    waas_list_access_response * list_access_response;
} waas_wallet_list_access_response;

static inline void waas_wallet_list_access_response_init(waas_wallet_list_access_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_list_access_response_free(waas_wallet_list_access_response *value) {
    if (!value) return;

    if (value->list_access_response) {
        waas_list_access_response_free(value->list_access_response);
        free(value->list_access_response);
        value->list_access_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}
typedef struct waas_wallet_revoke_access_request {
    waas_revoke_access_request * revoke_access_request;
} waas_wallet_revoke_access_request;

static inline void waas_wallet_revoke_access_request_init(waas_wallet_revoke_access_request *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_revoke_access_request_free(waas_wallet_revoke_access_request *value) {
    if (!value) return;

    if (value->revoke_access_request) {
        waas_revoke_access_request_free(value->revoke_access_request);
        free(value->revoke_access_request);
        value->revoke_access_request = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_revoke_access_response {
    waas_revoke_access_response * revoke_access_response;
} waas_wallet_revoke_access_response;

static inline void waas_wallet_revoke_access_response_init(waas_wallet_revoke_access_response *value) {
    if (!value) return;
    memset(value, 0, sizeof(*value));
}

static inline void waas_wallet_revoke_access_response_free(waas_wallet_revoke_access_response *value) {
    if (!value) return;

    if (value->revoke_access_response) {
        waas_revoke_access_response_free(value->revoke_access_response);
        free(value->revoke_access_response);
        value->revoke_access_response = NULL;
    }
    memset(value, 0, sizeof(*value));
}

typedef struct waas_wallet_client waas_wallet_client;

waas_wallet_client *waas_wallet_client_create(const char *base_url, const waas_client_options *options);
void waas_wallet_client_destroy(waas_wallet_client *client);
int waas_wallet_client_configure(waas_wallet_client *client, const waas_client_options *options);
int waas_wallet_client_send_prepared_request(
    waas_wallet_client *client,
    const waas_prepared_request *request,
    waas_http_response *response,
    waas_error *error
);
int waas_wallet_commit_verifier_prepare_request(
    const waas_wallet_commit_verifier_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_commit_verifier_parse_response(
    const waas_http_response *http_response,
    waas_wallet_commit_verifier_response *response,
    waas_error *error
);

int waas_wallet_commit_verifier(
    waas_wallet_client *client,
    const waas_wallet_commit_verifier_request *request,
    waas_wallet_commit_verifier_response *response,
    waas_error *error
);
int waas_wallet_complete_auth_prepare_request(
    const waas_wallet_complete_auth_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_complete_auth_parse_response(
    const waas_http_response *http_response,
    waas_wallet_complete_auth_response *response,
    waas_error *error
);

int waas_wallet_complete_auth(
    waas_wallet_client *client,
    const waas_wallet_complete_auth_request *request,
    waas_wallet_complete_auth_response *response,
    waas_error *error
);
int waas_wallet_create_wallet_prepare_request(
    const waas_wallet_create_wallet_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_create_wallet_parse_response(
    const waas_http_response *http_response,
    waas_wallet_create_wallet_response *response,
    waas_error *error
);

int waas_wallet_create_wallet(
    waas_wallet_client *client,
    const waas_wallet_create_wallet_request *request,
    waas_wallet_create_wallet_response *response,
    waas_error *error
);
int waas_wallet_use_wallet_prepare_request(
    const waas_wallet_use_wallet_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_use_wallet_parse_response(
    const waas_http_response *http_response,
    waas_wallet_use_wallet_response *response,
    waas_error *error
);

int waas_wallet_use_wallet(
    waas_wallet_client *client,
    const waas_wallet_use_wallet_request *request,
    waas_wallet_use_wallet_response *response,
    waas_error *error
);
int waas_wallet_sign_message_prepare_request(
    const waas_wallet_sign_message_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_sign_message_parse_response(
    const waas_http_response *http_response,
    waas_wallet_sign_message_response *response,
    waas_error *error
);

int waas_wallet_sign_message(
    waas_wallet_client *client,
    const waas_wallet_sign_message_request *request,
    waas_wallet_sign_message_response *response,
    waas_error *error
);
int waas_wallet_send_transaction_prepare_request(
    const waas_wallet_send_transaction_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_send_transaction_parse_response(
    const waas_http_response *http_response,
    waas_wallet_send_transaction_response *response,
    waas_error *error
);

int waas_wallet_send_transaction(
    waas_wallet_client *client,
    const waas_wallet_send_transaction_request *request,
    waas_wallet_send_transaction_response *response,
    waas_error *error
);
int waas_wallet_call_contract_prepare_request(
    const waas_wallet_call_contract_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_call_contract_parse_response(
    const waas_http_response *http_response,
    waas_wallet_call_contract_response *response,
    waas_error *error
);

int waas_wallet_call_contract(
    waas_wallet_client *client,
    const waas_wallet_call_contract_request *request,
    waas_wallet_call_contract_response *response,
    waas_error *error
);
int waas_wallet_list_access_prepare_request(
    const waas_wallet_list_access_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_list_access_parse_response(
    const waas_http_response *http_response,
    waas_wallet_list_access_response *response,
    waas_error *error
);

int waas_wallet_list_access(
    waas_wallet_client *client,
    const waas_wallet_list_access_request *request,
    waas_wallet_list_access_response *response,
    waas_error *error
);
int waas_wallet_revoke_access_prepare_request(
    const waas_wallet_revoke_access_request *request,
    waas_prepared_request *prepared_request,
    waas_error *error
);

int waas_wallet_revoke_access_parse_response(
    const waas_http_response *http_response,
    waas_wallet_revoke_access_response *response,
    waas_error *error
);

int waas_wallet_revoke_access(
    waas_wallet_client *client,
    const waas_wallet_revoke_access_request *request,
    waas_wallet_revoke_access_response *response,
    waas_error *error
);

#ifdef __cplusplus
} // extern "C"
#endif

#endif // WAAS_WAAS_HEADER_H